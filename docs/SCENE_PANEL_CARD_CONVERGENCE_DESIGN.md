# Scene Panel Card Convergence — the Properties body becomes real card rows

**Status: SHIPPED 2026-07-18 (all phases: C-P1a/b/c/d + C-P2, `wave/scene-card-convergence`).** Every scene param row (World/Object/Light/Camera/Lens/Modifier) now builds through `build_param_row`/`match_param_row_click`/`build_audio_mod_drawer` — one widget dialect, one drag protocol, one undo unit per gesture — per D1–D5. §3's full negative gate (`build_object_slider_row|build_mod_button|fn build_numeric_row|fn build_triplet_row|build_light_numeric_row|build_camera_numeric_row|build_modifier_numeric_row`, including comment mentions) is 0 hits in `scene_setup_panel.rs`. Landing report: `docs/landings/2026-07-18-scene-card-convergence.md`.

**C-P1d BUILT 2026-07-18 (session 12, `wave/scene-card-convergence`, slot-0) — Modifier family fully converted onto the card row core; CLOSES the design's §3 row-1 gate at 0 (the last bespoke-builder family).** Mirrors C-P1c: `build_modifier_card_row` (a running slot cursor into a VARIABLE-length `modifier_card: SceneCardState`, since a modifier stack has no fixed row union — see that field's own doc comment) replaces the deleted `build_modifier_numeric_row`/`build_modifier_enum_row`; Axis rows ride `value_labels` (no bespoke enum stepper) exactly like C-P1c's Light rows. `ModifierParamRowVm` promoted `RowValue`/`EnumRowValue` → `ModulatedRow`/`ModulatedEnumRow` (state_sync's `modifier_param_rows` threads `gen_inst`/`automation_latched` through, same `mrow` pattern every other family uses). Full bespoke-layer deletion swept clean: `object_steppers`/`object_value_cells`/`object_enum_cells` fields, `struct ValueDrag` + the panel's own `DragController<ValueDrag>`, and `value_cell_at()` (+ `app.rs`'s Priority 2d caller) are all deleted — Modifier was their only remaining producer. `build_mod_button` renamed to `build_expose_button` (the literal old name is itself in the negative-gate regex — same function, same behavior, every family). One real bug found by nextest, not review: `build_param_row`'s `row_key_base` collided between `object_card` and `modifier_card` (both start at slot 0, and unlike World/Light/Camera — mutually exclusive Properties-body sections — Modifier's rows build as Object's own siblings under the same `content_parent` every frame) — fixed with `MODIFIER_CARD_ROW_KEY_OFFSET`, a large disjoint slot offset. Gates: `cargo check`/`clippy -p manifold-ui -p manifold-app` clean; focused nextest (manifold-ui/app/editing/playback) 1637/1637 green; new `modifier_id_map_is_total_over_every_built_row` + `modifier_angle_card_row_sweeps_full_range_with_one_commit` (undo-granularity); §3 negative gate 0 hits including comments. Flows: all four required (`scene-setup-fog-density-card-row`, `scene-panel-card-convergence-c-p1b-object-scrub`, `scene-setup-select-updates`, `scene-setup-modifier-stack`) re-verified green — `scene-setup-modifier-stack.json`'s Drag target moved from the old `param1_value` (no longer drag-armable — only the track is) to a new `param1_track` automation name; two new flows (`scene-card-convergence-c-p1d-light-scrub-drawer.json`, `-roughness-scrub-drawer-undo.json`) prove the drawer-inline gates C-P1c waived. BUG-236 (stale `"Outliner"` selector) CLOSED — both its flows fixed; BUG-240 logged for `scene-setup-scrub-fine.json`'s now-fully-retired shift-fine gesture (not required to be green this session). New render-level test `roughness_driver_active_renders_the_drawer_inline` proves the driver-config drawer builds inline (the flow-script harness itself can't observe this — same BUG-234/BUG-239 gap family, noted, not re-litigated). Side-by-side dialect PNG looked at (`target/ui-snapshots/gltfscene/run-scene-panel-card-convergence-c-p1b-object-scrub/08.png`): Scene Setup Properties body and the generator card's own Audio Triggers rows read as the same widget — label / blue fill-bar / value / T·∿·A icon triplet — confirmed, not asserted.

**C-P2 (paper) DONE 2026-07-18, same session.** This status header; `docs/SCENE_PANEL_UX_DESIGN.md` cross-referenced (its D2/D3 rendering decisions were already marked superseded-by this design at C-P1a; confirmed still accurate, no stale phase claims found); supersession `rg` over `docs/` for the deleted builder names — 0 stale hits outside this file's own historical narration; `docs/BUG_BACKLOG.md` Status lines updated (BUG-236 closed, BUG-240 logged); `docs/VERIFICATION_DEBT.md` VD-032 closed as moot (its subject — the bespoke scrub-hairline draw — no longer exists), VD-033 opened as a note (not a gap) on C-P1c's sun-intensity render proof reading subtle to the eye despite being numerically real.

**C-P1a BUILT 2026-07-17** (session 3, `wave/scene-card-convergence`, slot-6) — Environment/Fog family fully converted, plumbing (D2 id map, D3 `SceneCardState`, D4 drag cadence) built and shared for later families' own sub-phases. Landed to main as part of this design's overall SHIPPED close (2026-07-18). Gates: focused nextest + scoped clippy green; undo-granularity, id-map-totality, and imported-def-write proven by NEW Rust tests (`inspector.rs::scene_card_convergence_tests`, `scene_setup_panel.rs::world_id_map_is_total_over_every_built_row`); `scripts/ui-flows/scene-setup-fog-density-card-row.json` green 13/13 through the REAL dispatch path (scrub the real card slider, driver/expose buttons present); PNG pair looked at (before/after track fill — see session report). Found+fixed a real correctness bug in the process: a naive self-capturing `SetGraphNodeParamCommand` at commit time would have captured the POST-drag value as "previous" (the live ticks already advanced the graph past it) — fixed by adding `SetGraphNodeParamCommand::with_previous(..)` so the commit seeds the TRUE pre-drag value instead of self-capturing. Found (not fixed, logged as BUG-239): the headless `--script` harness shows a stale value in its PNG/dump after this exact dispatch shape, even though the write is proven correct at the Rust dispatch level and independently at the flow-script's own dispatched-action log — isolated to the harness's invalidation/resync plumbing, not the dispatch code. C-P1b (Object family) / C-P1c (Light+Camera+Lens) / C-P1d (Modifier + full bespoke-layer deletion + flow updates) remain, per the phasing amendment below. Session 1 found and value-proved that Camera/World/Light param addressing/dispatch is correct for a fresh layer (BUG-237, still open pending C-P1c/d's own families), and that the "dead" eye toggles on those rows are an intentional dimmed-placeholder per SCENE_PANEL_UX D5, not a bug (BUG-238) — the eye-slot amendment below fixes that when C-P1c/d convert those families; C-P1a's own Environment/Fog rows never carried an eye affordance to begin with (World has no `visible` param), so this amendment doesn't apply to what shipped this session.

**C-P1c BUILT 2026-07-18 (session 11, `wave/scene-card-convergence`, slot-0) — Light + Camera + Lens families fully converted onto the card row core; CLOSES BUG-237 and BUG-238.** Mirrors C-P1b exactly: `build_light_card_row`/`build_camera_card_row` (new `LIGHT_ROW_*`/`CAM_ROW_*` fixed-slot consts, `light_card`/`camera_card: SceneCardState`, `sync_light_modulation`/`sync_camera_modulation`) replace the deleted bespoke `build_light_numeric_row`/`build_light_triplet_row`/`build_light_enum_row`/`build_camera_numeric_row`/`build_camera_triplet_row` and their value-cell offset consts + `light_value_cells`/`light_steppers`/`light_enum_cells`/`camera_value_cells`/`camera_steppers` fields (`rg` confirms 0 live hits, 1 explanatory doc-comment mention). Enum rows (Mode/Cast Shadows/Shadow Softness) needed NO bespoke dropdown builder — `build_param_row`'s existing `ParamInfo.value_labels` path already renders a labeled, drag/type-in-armed row (D1's "check for a card enum row first" — it existed). `LightKnownRow`/`LensRowVm`/`OrbitCameraRowVm`/`FreeCameraRowVm`/`LookAtCameraRowVm` fields promoted `RowValue`→`ModulatedRow` (Mode/Cast Shadows/Shadow Softness → a new `ModulatedEnumRow`, kept separate from Modifier's still-`RowValue`-based `EnumRowValue` so C-P1d's own scope isn't dragged forward early); `state_sync.rs`'s local `enum_row`/`row` closures updated to wrap in `mrow`. **Eye-slot amendment (BUG-238):** `EyeSlot::Dimmed` deleted outright; Camera/World/Light rows (and Custom Light rows) now render `EyeSlot::Empty` — no glyph, same reserved width; Custom Object rows keep the dimmed glyph (Object DOES carry `visible`, just not addressable for that instance). Dispatch needed NO new `inspector.rs` branches — `resolve_scene_param`/`dispatch_inspector`'s three arms were already family-agnostic since C-P1a (a single per-family-card `.or_else` chain, not per-family branches; only `resolve_scene_param` itself grew two more `.or_else`s for `light_card`/`camera_card`). Unlike C-P1b's Object family, Light/Camera write addresses are root-scoped by construction (`state_sync`'s `row`/`enum_row` closures always use `RowAddr::root`, lights/camera are never grouped) — no D12-style group-scope bug class applies here.

**BUG-237 closed with a render-level mechanism proof, not just dispatch coverage** (the design's own §"expected to CLOSE BUG-237" gate): two halves. (1) Dispatch-level: `inspector.rs::scene_card_convergence_tests::light_intensity_commit_writes_the_layer_instance_def_at_root_scope` / `camera_orbit_commit_writes_the_layer_instance_def_at_root_scope` drive a REAL `ParamSnapshot`→`ParamChanged`→`ParamCommit` sequence against a fresh SceneStarter layer and read the committed value back out of `layer.generator_graph()` — proves the write reaches the exact def the renderer reads. (2) Render-level: new `crates/manifold-renderer/tests/gpu_proofs/bug237_light_camera_commit_render_proof.rs` (gpu-proofs feature) renders SceneStarter before/after a def mutation shaped exactly like `SetGraphNodeParamCommand::execute`'s own write, through the real `PresetRuntime`, and the session LOOKED AT the resulting PNG pair (`/tmp/bug237_sun_intensity_{before,after}.png`, `/tmp/bug237_camera_orbit_{before,after}.png`): sun intensity 1.0→8.0 visibly brightens the cube (mean_abs_diff 0.09); camera orbit +π/2 visibly reframes it a quarter-turn, ground-plane lines rotating with it (mean_abs_diff 0.06). Root cause confirmed as hypothesis (a) from BUG-237's own "what remains unproven" list — the bespoke per-family click/drag routing this phase deleted; hypotheses (b) (stale node ids) and (c) (content-thread re-render) are ruled out by the render proof succeeding on the first try with no other change.

Gates: focused nextest (`manifold-ui`/`manifold-app`/`manifold-editing`/`manifold-playback`, 1636 passed, 0 failed) + scoped clippy (`-p manifold-ui -p manifold-app -- -D warnings`, per CLAUDE.md's mandated invocation) green; undo-granularity (`light_intensity_card_row_sweeps_full_range_with_one_commit`, UI-level; the dispatch-level tests above independently prove exactly one `Execute` on release) and id-map-totality (`scene_setup_panel.rs::light_id_map_is_total_over_every_built_row` / `camera_id_map_is_total_over_every_built_row`) both pass. `camera_row_is_angle` structurally derives D10's degrees table (orbit/tilt/yaw/pitch/roll/fov_y) from the row's FIXED slot, same convention C-P1b established for Object's Rotation rows — `camera_orbit_commit_writes_the_layer_instance_def_at_root_scope` explicitly asserts the dispatched/committed payload stays RAW RADIANS (degrees is a display/drag-scaling concern only, never a storage conversion). Negative gates: deleted-symbol `rg` → 0 live hits; `rg 'Arc<Mutex\|Arc<RwLock'` → 0 in both touched files; `GraphParamTarget` untouched (diff stat: only `scene_setup_panel.rs` + `state_sync.rs` touched). **Shortcuts taken, stated plainly:** the flow-script (`scripts/ui-flows/*.json`) scrub-a-light-row-and-open-its-drawer-inline gate and the side-by-side dialect PNG (Light/Camera Properties body vs. an effect card, `ui_snap`-shaped) were NOT run this session — time-boxed out in favor of the render-level BUG-237 proof, which the task treated as the higher-value gate. This is real debt: C-P1d's own session should either add them or explicitly waive them, not silently inherit the gap. `light_enum_row_click_does_not_emit_scene_setup_enum_clicked` and `shadow_softness_value_cell_click_opens_dropdown_mode_does_not`'s replacement are UI-dispatch-only proof that the old P4/D9 dropdown-on-click mechanism is correctly ABSENT for Light now (superseded by the card's own value_labels path), not a flow-script proof. C-P1d (Modifier family + full bespoke-layer deletion) picked up the waived flow-script and PNG gates, per its own status block above — DONE 2026-07-18.

**C-P1b BUILT 2026-07-17 (session 10, `wave/scene-card-convergence`, slot-3) — Object family fully converted onto the card row core, reusing C-P1a's plumbing.** Position/Rotation/Scale (9 rows) + Color (3 rows, swatch kept) + Metallic/Roughness (2 rows) = 14 fixed slots (`OBJ_ROW_POS_X`..`OBJ_ROW_ROUGHNESS`) in a new `object_card: SceneCardState`, built by `build_object_card_row`/`build_object_color_rows`. Triplet layout: no compact multi-cell precedent existed in `param_card.rs` (checked per the brief), so each axis is its own full-width card row ("Position X"/"Position Y"/"Position Z", not a shared-label 3-cell strip) — the committed shape. Rotation uses `is_angle: true` structurally (not `is_degrees_param`-derived) — `format_param_value`'s is_angle branch rounds to whole degrees (`"90°"`), a legitimate display-format change from the old bespoke one-decimal formatter. Deleted: `build_triplet_row`, `build_color_row`, `build_object_slider_row`, the `ObjSlider` enum, `metallic_slider`/`metallic_slider_row`/`roughness_slider`/`roughness_slider_row` fields and their dispatch code, and the value-cell offset consts (`OBJ_OFF_POS_X`/`ROT_X`/`SCALE_X`/`COLOR_R`/`METALLIC`/`ROUGHNESS`, `triplet_cell_automation_name`, `object_slider_row_automation_name`) — `rg` confirms 0 live hits (2 doc-comment mentions of the deleted names remain, explanatory only). `build_mod_button` (the exposure-lane helper) was KEPT, not deleted — it's still load-bearing for World (C-P1a, already shipped) and the unconverted Light/Camera/Modifier families; only its Object-specific value-cell call sites moved onto the card row's own exposure lane. This is a deliberate deviation from the literal "build_mod_button... DELETED" instruction in this phase's brief — full deletion is C-P1d's own gate (`§3` row-1 rg), once every family is converted; deleting it now would have broken three still-live families. Escalated in spirit (documented here, not asked live) rather than silently improvised.

Two real bugs found and fixed in the process (not present in C-P1a, both structural gaps this family's own scoping/shape exposed):
1. **`read_scene_node_param` was root-scope-only** (C-P1a's own doc comment: "a non-empty scope returns None rather than guessing"). `AddSceneObjectCommand` nests `node.transform_3d` AND the material node INSIDE the object's own group (verified by reading the command's `execute()`), so every Object row's `ParamCommit` has a non-empty `scope_path` — the C-P1a-shaped `read_scene_node_param` would have silently no-op'd EVERY Object commit. Fixed by `descend_to_scope` (mirrors `manifold-editing`'s private `descend_level`), proven by two new dispatch-level tests using the REAL `AddSceneObjectCommand` (`inspector.rs::scene_card_convergence_tests::object_position_x_commit_writes_the_layer_instance_def_at_group_scope` / `object_roughness_commit_writes_the_layer_instance_def_at_group_scope`).
2. **The Click handler's `match_param_row_click`/`mod_tab_hit` dispatch chain only ever checked `world_card`** — Object rows' Driver/Envelope/Audio arm buttons and mod-tab-strip clicks were completely dead (every click fell through to the enum-cell/mod-button/no-op arms below). Found by actually running the acceptance flow (`scene-panel-card-convergence-c-p1b-object-scrub.json`) against the real `gltfscene` fixture and noticing no `DriverToggle` appeared in the dispatch log despite a driver-button click landing on a real node. Fixed by adding the same `match_param_row_click`/`mod_tab_hit` branch pair for `object_card`, mirroring `world_card`'s.

Gates: focused nextest (`manifold-ui`/`manifold-app`/`manifold-editing`/`manifold-playback`, 1632 passed) + scoped clippy green; undo-granularity (`object_position_x_drag_session_yields_exactly_one_undo_entry`), id-map-totality (`scene_setup_panel.rs::object_id_map_is_total_over_every_built_row`), and imported-def-write AT GROUP SCOPE (the two tests above, using the real `AddSceneObjectCommand` — the actual D12 grouped shape, not a synthetic root-scoped fixture) all pass. Flow `scripts/ui-flows/scene-panel-card-convergence-c-p1b-object-scrub.json` green 13/13 (scrubs Roughness, opens its driver drawer — `DriverToggle` confirmed dispatched — scrubs Position X). `scene-setup-ux-p2-demo.json` rewritten (its raw-pixel-coordinate drag had silently drifted onto the WRONG row after the layout change from compact triplets to full-width rows — replaced with named `_track` selectors) and re-verified 8/8. `scene-setup-select-updates.json` and `scene-setup-modifier-stack.json` (22/22) re-verified unaffected. Found (not fixed, logged as BUG-236): `scene-setup-add-object.json` and `scene-setup-scrub-fine.json` both fail at a pre-existing stale `"Outliner"` text assertion, unrelated to this phase. PNG (`target/ui-snapshots/gltfscene/run-scene-panel-card-convergence-c-p1b-object-scrub/08.png`) shows the converged Object Properties body beside a generator card — same row dialect (label / blue fill-bar / value / T·∿·A icons) confirmed by inspection. Known harness limit (BUG-239) still applies to displayed values in `--script` PNGs; the write-correctness gates above carry that half at the Rust dispatch level. C-P1c (Light+Camera+Lens) / C-P1d (Modifier + full bespoke-layer deletion + flow updates) remain.

**Session 2 (2026-07-17, resumed) findings — D2's two `⚠ VERIFY-AT-IMPL` questions are now answered, read-only, no code changed:**
1. **D2's routing target already exists and needs no new mechanism.** `resolve_graph_target(GraphParamTarget::Generator, …)` (`crates/manifold-app/src/ui_bridge/inspector.rs:221`) already resolves to `manifold_core::GraphTarget::Generator(lid)` off `active_layer` — the *exact* `GraphTarget` `SetGraphNodeParamCommand::new(target, node_id, param_name, new_value, catalog_default)` (`crates/manifold-editing/src/commands/graph.rs:793`) takes. So a card-shaped `PanelAction::ParamChanged(GraphParamTarget::Generator, synthesized_id, val)` dispatched from a scene row can resolve its `GraphTarget` through the SAME call cards already use — no bespoke resolver needed for the target half of the address. What's still missing: `dispatch_inspector`'s `ParamSnapshot`/`ParamChanged`/`ParamCommit` arms (`inspector.rs:1083,1119,1145`) address by `param_id` into `project.with_preset_graph_mut` (a *preset-instance* slot lookup) — incompatible with scene rows' `(scope_path, node_doc_id, param_id)` triple, which is a raw graph-node address (`SetGraphNodeParamCommand::with_scope`, not a preset param slot). Each of those three arms needs a new first branch: if `param_id` resolves through the scene panel's per-frame id map (D2's `AHashMap<ParamId, RowAddr>`) to a `RowAddr`, route through `SetGraphNodeParamCommand` addressed by that `RowAddr` instead of falling into `with_preset_graph_mut`. `ui: &mut UIRoot` is already a parameter of `dispatch_inspector`, and `ui.scene_setup_panel` (`crates/manifold-app/src/ui_root.rs:212`) is the field to host the map/lookup method — reachability is not a gap, the branch itself just isn't written.
2. **D4's "commit-with-prior-value" concern is already satisfied by the existing command — the gap is call cadence, not command shape.** `SetGraphNodeParamCommand::execute()` self-captures `previous_value: Option<Option<SerializedParamValue>>` on first execute and restores it on undo (`graph.rs:793-853`) — it already IS a commit-with-prior-value command. The undo-spam Peter reported is NOT a missing command shape; it's that `PanelAction::SceneSetupParamChanged`'s current dispatch arm (`project.rs:260`) calls `boxed.execute(project)` + `ContentCommand::Execute` on **every** drag-move event, so every motion event pushes its own undo entry with its own self-captured `previous_value` at that instant. The fix is cadence, mirroring the card's existing split: `ParamChanged`-equivalent motion writes go through a live, non-undoable path (`ContentCommand::MutateProjectLive`, already used by the card's own `ParamChanged` arm at `inspector.rs:1134`) and only `ParamCommit`-equivalent (drag-end) pushes the one real `SetGraphNodeParamCommand` `Execute`. No escalation triggers here — D4's own escalation clause ("commit-with-prior-value shape genuinely lacking") does not fire; this is ordinary wiring work.
3. **ScenePanel already runs its own drag state machine** (`drag_layer_id: Option<LayerId>` + per-event `SceneSetupParamChanged` dispatch, `scene_setup_panel.rs:982` onward) that would need to become a `DragController<ParamDragTarget>`-shaped Begin/Changed/Commit sequence (reusing the type from `param_card.rs`) emitting card-shaped actions instead of the current one-shot `SceneSetupParamChanged` per pointer-move.
4. **Why this session still shipped no family:** converting even the smallest family (Environment/Fog, 4 rows, no enum/axis rows) to `build_param_row` correctly requires, in one coordinated change: (a) `ScenePanel` growing a per-frame `Vec<ParamInfo>` + `AHashMap<ParamId, RowAddr>` (D2/D3) built during row layout; (b) `ScenePanel` growing its own `SliderNodeIds`/`ParamRowIds` arrays (`SceneCardState`, D3) and calling `build_param_row`/`match_param_row_click` instead of `build_numeric_row`; (c) `ScenePanel` replacing its ad hoc drag fields with the `ParamDragTarget` protocol (D4, now scoped per point 2/3 above); (d) the three new `dispatch_inspector` branches (point 1); (e) deleting the family's now-dead bespoke code; (f) new id-map-totality + undo-granularity tests; (g) a render-level before/after PNG proof, looked at. Each sub-piece is now well-scoped (nothing is architecturally blocked — no escalation triggers fired), but committing (a)–(g) correctly, verified, for even one family, in one sitting risks the exact "half-converted family" and "risk to already-working card dispatch" outcomes both this doc and CLAUDE.md forbid. Re-run C-P1 as a fresh, fully-resourced session armed with points 1–3 above (skip re-deriving them) and build (a)–(g) for Environment/Fog first, per the doc's own family order.
**Orchestrator amendment (2026-07-17, added mid-session):** the eye-slot trailing affordance (§2 D1, "Rows that aren't params… remain panel chrome") gets an override on top of SCENE_PANEL_UX D5: rows whose scene item has no real visible/enable param (Camera/World/Light — confirmed via `scene_vm.rs`, none of `SceneLightVm`/`CameraVm`/`EnvironmentVm`/`AtmosphereVm` carry a visibility address) render an EMPTY trailing slot, not a dimmed glyph. Only Object rows (which carry `scene_object.visible`) keep the live eye. Not yet implemented — folds into C-P1's eventual build.
**Prerequisites:** SCENE_PANEL_UX UX-P1..P3b-i (SHIPPED `76251784`). Supersedes SCENE_PANEL_UX's D2/D3 rendering decisions and RESOLVES its blocked UX-P3b-ii.
**Execution contract:** read docs/DESIGN_DOC_STANDARD.md §5–§6 before starting any phase.

Peter, 2026-07-17, looking at the shipped P3a/P3b-i panel: *"Why did you build new custom input and modulation buttons and infra? We should just use the same widgets etc from the cards. This is bugged like crazy already, we should just keep the interaction models and UI and elements and systems from the existing infra please."* And on undo: *"The undo and redo node to operate over click and release of params, not every single tiny step."*

The root cause is a design error in SCENE_PANEL_UX, stated here so nobody repeats it: its D2/D3 said shape the rows **like** the card's rows, which produced a third widget dialect — lookalike mod buttons, lookalike sliders, panel-local click routing, per-mouse-move undo entries — each subtly diverging from the card's real interaction model. Two execution sessions independently refused to bolt card drawers onto those rows; that refusal was this design being asked for. The fix: **the panel's parameter rows ARE the card's rows** — same builder, same click router, same drag protocol, same drawers, same mod buttons — and the bespoke row builders are deleted. Chrome that is genuinely panel-shaped (outliner, name field, eye toggles, Add-modifier dropdown, section labels) stays.

On stage: every parameter in the app scrubs, types, resets, exposes, and modulates identically — one interaction model to learn — and a scrub is one undo step, so mid-set undo takes back the gesture, not the last half-pixel of it.

## 1. Audit — what exists (verified 2026-07-17, against `76251784`)

| Piece | Where | State |
|---|---|---|
| The shared row subsystem | `build_param_row` (param_slider_shared.rs:2223), `match_param_row_click` (:2705), `build_audio_mod_drawer` (:1714) | The real widgets. Host inputs: `ParamInfo`, `ParamModState`, slot index `i`, `GraphParamTarget`, `SliderColors`, `label_width`, `active_tab`, compact flag |
| Third-consumer precedent | `audio_trigger_section.rs` | Proves the row layer hosts outside the two cards |
| `ParamInfo` | param_card.rs:97 | `param_id: ParamId` (an OWNED string id is already a supported case — user-tier effect params), `min/max/default`, `whole_numbers`, `is_angle`, `exposed`, toggle flag |
| Card drag protocol | slider.rs `SliderDragState` + the card dispatch arms | Begin (snapshot) → live Changed → Commit on release = ONE undo unit. The scene panel's bespoke scrub instead dispatches a command per motion event — Peter's undo complaint |
| Per-param mod lookup | `lookup_param_mod_for_id` (state_sync.rs:2500, landed by C5) | Exactly the query card-row hosting needs; currently `#[allow(dead_code)]` with this design as its named un-suppression trigger |
| Exposure mechanism | P3a: `SceneSetupExposeParam` → `ToggleNodeParamExposeCommand`, named `<Owner> · <Label>`; round-trip proven (manifold-playback tests) | SURVIVES unchanged — this design replaces rendering, not mechanism |
| Scene addressing | `RowAddr { scope_path, node_doc_id, param_id }` (scene VM) vs. card addressing `(GraphParamTarget, ParamId, i)` | The one genuine seam this design must decide (D2) |
| Bespoke layer to delete | scene_setup_panel.rs: `build_object_slider_row`, `build_mod_button`, `build_numeric_row`/`build_triplet_row`/`build_light_*_row`/`build_camera_*_row`/`build_modifier_numeric_row` value/slider/mod chrome, `value_cell_at` hover plumbing, scrub-hairline draw | The third dialect. Deletion gates in §4. (`value_cell.rs` the GESTURE contract stays — other docks use it; the scene panel just stops being a consumer for param rows) |
| C5's sizing inventory | SCENE_PANEL_UX "as attempted" note | 8 parallel per-row arrays + card-lifetime state (`mod_active_tab`, copy-flash) — the honest cost, now committed as D3's `SceneCardState` |

## 2. Decisions

**D1 — Scene param rows render and interact through `build_param_row`/`match_param_row_click`/`build_audio_mod_drawer`, full stop.** Every parameter row the Properties body shows — object transforms, color components, Metallic/Roughness, Environment/Fog, Light, Camera, Lens, Modifier params — is built by the shared builder from a synthesized `ParamInfo`. The drawers open inline in the panel (this IS UX-P3b-ii, no longer blocked); the mod/driver/envelope buttons are the card's own components with the card's own lit/driven states. Rows that aren't params (name field, eyes, Add-modifier, enum/axis steppers where the card has no equivalent — check for a card enum row first and use it if one exists, escalate if genuinely absent) remain panel chrome.
*Rejected: keeping any bespoke param-row builder for "simple" rows* — the mixed dialect is the bug class being deleted.

**D2 — Addressing rides synthesized owned `ParamId`s + a per-frame panel map; `GraphParamTarget` is untouched.** `ParamInfo.param_id` already supports owned ids. The panel synthesizes a stable id per row (opaque, e.g. `scene.{node_doc_id}.{param}` with scope disambiguation), builds a frame-local `AHashMap<ParamId, RowAddr>` alongside the row list, and the scene dispatch arms resolve incoming card actions through that map to the existing scene command routing. When a row IS exposed, its id is the exposure's real id, so drawers/modulation hit the existing card param directly and `lookup_param_mod_for_id` supplies `ParamModState`.
*Rejected: adding a `SceneNode` variant to `GraphParamTarget`* — the enum is `Copy` and flows through dozens of dispatch arms; a `Vec<u32>` scope path breaks that for one consumer. *Rejected: eager-exposing every visible row so all ids are real exposures* — floods the card and the perform surface with params nobody chose (SCENE_PANEL_UX D8's standing rejection).

**D3 — The panel owns a `SceneCardState`, shaped like the cards' own state.** The 8 parallel arrays + `mod_active_tab` + copy-flash that C5 inventoried, rebuilt per selection change exactly as the panel's VM already is (D1 of SCENE_PANEL_UX: no staleness), populated via `lookup_param_mod_for_id`. No scene-local driven cache beyond it — driven visuals read the same facts the cards read.

**D4 — Drag = card protocol = one undo unit per gesture.** Scene rows adopt the Begin/Changed/Commit drag session the card sliders run: pointer-down snapshots, motion updates live (through the existing live-param path the panel's value writes already use), release commits ONE command carrying before→after. Type-in and right-click-reset commit single units as they already do on cards. This kills the per-motion undo spam for every scene param at once.
Consequences, stated honestly: mid-drag values are live on the content thread before the commit lands (same as cards — that's the shipped semantic, not new risk). If the panel's current per-event command path can't express Begin/Commit without a new command, reuse the exact command pair/shape the card dispatch uses — escalate if the scene command family genuinely lacks a commit-with-prior-value shape rather than inventing a third.

**D5 — Unbounded params get their `ParamDef` range; where none exists, this doc fixes the soft range.** `ParamInfo` requires min/max. Use the param's own `ParamDef::range` wherever defined (`⚠ VERIFY-AT-IMPL: rg the transform_3d / light / camera ParamDefs for `range:` — most scene params have one`). For any genuinely rangeless param: position ±10.0, scale 0.01..10.0, rotation ±360° (display degrees, storage radians via `is_angle`). Type-in accepts values beyond the soft range (the card's existing text-entry behavior governs); the soft range only shapes the slider track.

## 3. Invariants & enforcement

| Invariant | Enforcement |
|---|---|
| No bespoke param-row builders remain | negative gate: `rg -n 'build_object_slider_row|build_mod_button|fn build_numeric_row|fn build_triplet_row|build_light_numeric_row|build_camera_numeric_row|build_modifier_numeric_row' crates/manifold-ui/src/panels/scene_setup_panel.rs` → 0 hits |
| One undo unit per scrub gesture | unit test: drag session Begin→3×Changed→Commit produces exactly one undo entry; undo restores the pre-drag value (extends the card's own drag-protocol test pattern) |
| Card and panel rows share one code path | `build_param_row` call sites in scene_setup_panel.rs resolve into param_slider_shared.rs (positive `rg` in the phase report); no copied builder bodies |
| Exposure round-trip still holds | existing manifold-playback round-trip tests stay green untouched |
| Panel id map is total | debug_assert + unit test: every built row's synthesized id resolves through the map back to its RowAddr |

## 4. Phasing

Test scope per phase: `cargo nextest run -p manifold-ui -p manifold-app -p manifold-editing -p manifold-playback`; scoped clippy; full sweep at landing. No GPU runs (no shader/kernel).

### Phasing amendment (2026-07-17, after two execution sessions stopped short)

C-P1 as one session bundled seven coordinated pieces across the shared dispatch path; two sessions correctly refused it. Split, using session 2's resolved findings (design-doc status note, points 1–3: `resolve_graph_target` is shareable; `SetGraphNodeParamCommand` already self-captures `previous_value` — the undo bug is dispatch CADENCE, per-event `Execute` instead of the card's `MutateProjectLive`-during-drag + one `Execute` on release; the three insertion points are `inspector.rs:1083/1119/1145`):

- **C-P1a — plumbing + Environment/Fog — BUILT 2026-07-17 (session 3).** Build ONCE: the panel id map (D2), `SceneCardState` (D3), the three `dispatch_inspector` branches, the card drag cadence for scene rows (D4). Convert ONLY the Environment/Fog family (4 rows) end-to-end: `build_param_row` rendering, `match_param_row_click` routing, inline drawer, bespoke-builder deletion for that family, id-map-totality + undo-granularity tests, imported-def value test, before/after fog-density render proof (looked at). Gate: the family is COMPLETE by every §3 invariant scoped to it; all other families untouched and still working via their old path (transitional state is acceptable ONLY between sub-phases of this split, and the old path per family dies in its own sub-phase — no family is half-converted, ever).
- **C-P1b — Object family** (transforms, color, Metallic/Roughness, eye per the amendment) — reuses C-P1a's plumbing; adds the light Object eye verification.
- **C-P1c — Light + Camera + Lens families — BUILT 2026-07-18 (session 11).** (+ empty trailing slots per the eye amendment; render-level light-intensity + camera-orbit proof — CLOSED BUG-237 and BUG-238, see status header above.)
- **C-P1d — Modifier family + full bespoke-layer deletion — BUILT 2026-07-18 (session 12).** (§3 row-1 rg gate reached 0, including comment mentions; the four flow-script re-verifications + two new drawer-inline flows + a render-level driver-drawer test — see status header above for the doc's original "scrub → drawer → driver → ONE undo" acceptance flow's actual shape, adjusted for real undo-stack semantics DriverToggle turned out to have.)
- **C-P2 — DONE 2026-07-18, same session** (polish + paper — see status header).

### C-P1 — the row swap (superseded by the split above; original brief kept for the deliverable inventory)
- **Entry state:** `76251784`+. Re-verify: build_param_row signature (param_slider_shared.rs:2223), lookup_param_mod_for_id (state_sync.rs:2500), the card drag-protocol dispatch arms, P3a's expose dispatch (ui_bridge/project.rs).
- **Read-back:** this doc whole; param_card.rs + param_slider_shared.rs module docs; the card's drag Begin/Changed/Commit arms end-to-end; DESIGN_DOC_STANDARD §5–§6.
- **Deliverables:** D1 row swap for ALL param-row families; D2 id map; D3 `SceneCardState`; D4 drag protocol; D5 ranges; deletion of the bespoke layer (§3 row 1); un-suppress `lookup_param_mod_for_id`; updated flow scripts (`scene-setup-select-updates`, `scene-panel-ux-p3a-expose-modulate`, `scene-panel-ux-p3b-i-expose-light`, `scene-setup-modifier-stack` — selectors move to the card-row widget names); the §3 undo-granularity and id-map tests.
- **Gate:** all §3 rows; all four flows green; focused nextest + scoped clippy.
- **Acceptance demo:** L3 — a NEW flow: scrub Roughness via the real card slider in the panel, open its drawer inline, toggle a driver, then undo ONCE and assert the pre-scrub value returns. PNGs of the converged Properties body (cards' visual language, drawers open inline) — orchestrator reviews against the effect card side-by-side for dialect drift.
- **Performer gesture:** grab an object's Position, scrub it around, hit undo once — the object returns to where the gesture started. The flow asserts exactly this.
- **Forbidden moves:** keeping ANY bespoke row builder alive "for the simple rows"; a second drag protocol; parsing scene addresses out of id strings (the map is the resolver); touching `GraphParamTarget`; eager exposure; modifying playback modulation.

### C-P2 — polish + paper (one session)
- **Deliverables:** affordance-legibility PNG pass (label widths, drawer tab strip fit in the narrow dock — the doc grants `label_width`/compact-mode tuning ONLY via build_param_row's existing host inputs); SCENE_PANEL_UX_DESIGN.md status updated (D2/D3 superseded-by note, UX-P3b-ii resolved-by note); VERIFICATION_DEBT sweep; landing report.
- **Gate:** flows re-green; PNG review; supersession `rg` for the old builder names across docs/ + memory → fixed or tombstoned.

## 5. Decided — do not reopen
1. Panel param rows = card rows, one builder, no exceptions for "simple" rows (D1).
2. Addressing = synthesized owned ParamIds + panel map; `GraphParamTarget` untouched (D2).
3. One undo unit per gesture via the card drag protocol (D4).
4. Exposure-on-demand mechanism survives as-is; drawers now open inline AND on the card — same underlying config (D1+P3a).
5. Soft ranges per D5 where ParamDefs are silent.

## 6. Deferred
- **Color picker** — unchanged from SCENE_PANEL_UX (revive with a second consumer).
- **Card-side grouping of scene exposures** — revive if the generator card's list gets noisy in real use (SCENE_PANEL_UX D8's recorded consequence).
