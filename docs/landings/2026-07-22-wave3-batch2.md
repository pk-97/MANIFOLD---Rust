# Landing report — Wave 3 batch 2 (P3-D table-ization · P3-A call-shape)

**Landed:** 2026-07-22, lane/wave3-batch2 → main (claim on the bus, wave3 top session). First SEMANTIC batch of the campaign — every slice reviewed by the top session before the next started.
**Design:** `docs/RENDERER_RUNTIME_DECOMPOSITION_DESIGN.md` D8/D9. Commits: S0 9aa9cbfd (harness) · T1 a5bfb58e (MAP_FAMILIES) · T2 4bfd2168 (MATERIAL_PARAMS) · A1 dfed2524 (ImportCtx) · A2 dd751c23 (StandaloneKernelSpec) · A3 fa3dc166 (ChainBuildInputs + FrameContextInputs).

## What landed

- **P3-D:** the importer's two facts-wearing-code catalogs are typed const data in `gltf_import/materials.rs` — `MAP_FAMILIES` (16 rows, one per glTF map family; base-color stays explicit code with BOTH preserved oddities: the lone `textures_wired` increment, no `map_tex_cache` participation) and `MATERIAL_PARAMS` (32 rows; the 5 computed sites stay explicit, named). Adding a KHR texture family or material param = adding one row. `object_group.rs` 1,351 → 965 lines.
- **P3-A:** `wire_map_texture` (13 args) died into T1's walk; `build_object_group` 12→5 params via `ImportCtx`; `generate_standalone_ext` DELETED (single spec-taking `generate_standalone`); `try_build` 9→2 via `ChainBuildInputs`; `set_frame_context` 7→1 via `FrameContextInputs`. KEEPs honored (card_param, _with_device pair, is_compatible, run). **Zero production `too_many_arguments` allows remain in the three module trees.**
- **INV-R8 harness** (permanent): `scripts/gltf_def_capture.py` + `render_import --dump-def` + `Serialize` on ImportReport (the one sanctioned non-move edit).

## Gates

- **INV-R8 def-JSON equivalence:** 145 fixtures (139 real defs + 6 stable import-error sentinels), pre-vs-post BYTE-IDENTICAL at T1, T2, and A1 — manifest sha256 unchanged end-to-end (`66b0ae37…d895`); includes BOTH held-out fixtures (the_rosetta_stone, AMG GT3), run against the main-checkout fixture root, loud-fail on missing (never if-present skip).
- **Byte gate (codegen):** `fused_wgsl_snapshot_unchanged` PASS, freeze+codegen suite 102/102, reference goldens unmodified — supersedes gpu-proofs execution per D4/D-45 (the wave's single run is P3-Z).
- **Per-slice:** both-config cargo check (W3-D5) + clippy `-D warnings` + focused nextest, green at every slice; INV-R9 (tables module-private, rg-negative) and INV-R10 (no config files) green.
- **Landing (one run, build lock):** `cargo nextest run --workspace` → **3852 passed (1 leaky, pre-existing), 13 skipped** · `cargo clippy --workspace --tests -D warnings` → clean · `cargo deny check bans` → ok · LiveSchool oracle `cargo test -p manifold-io --test load_project` → **16/16**.

## Deviations / rulings

1. A2 deletion gate rescoped by the top session: `rg 'generate_standalone_ext\('` → 0 call/def sites; the two test-FN NAMES keep (renaming them would break the gpu-LIST identity gate, which outranks).
2. D9's "external set_frame_context callers" line was wrong — those hits are doc-comments of GeneratorInput's same-named method; the census site (PresetRuntime's) has zero external production callers. Scope confirmed to the census site.
3. Import-error fixtures captured as `{"import_error": …}` sentinels (exit 0) — error behavior is frozen into the equivalence gate too (strictly stronger than excluding them).

## Level reached

**L1** (target for behavior-preserving refactors; INV-R8's byte-diff is the observable artifact and is quoted above). Verification debt: none opened. gpu-proofs: deferred to P3-Z by design (D-45); the gpu-test corpus compiles under `--features gpu-proofs` at every slice (W3-D5).

## Click-script for Peter (≤2 min)

1. Import any .glb (or open a project with one) — identical placement, materials, cards (byte-identical def is a stronger claim than any visual check).
2. Play a clip with an effect chain — unchanged (chain build reshaped only in signature; oracle + workspace suite green).

## Next

P3-Z (batch 3): godfile_regrowth ceilings, D7 house rules onto the register, supersession sweep, FREEZE_COMPILER_MAP size refresh, and the wave's ONE gpu-proofs execution.
