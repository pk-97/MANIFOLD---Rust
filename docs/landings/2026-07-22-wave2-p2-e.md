# Landing report — Wave 2 P2-E: effects.rs → effects/ (2026-07-22)

**Design:** `docs/MODEL_COMMAND_DECOMPOSITION_DESIGN.md` §2 D4/D6, §4 P2-E (D-28/D-29 overnight mandate; Peter's morning review pending).
**Lane:** `lane/wave2-exec` (slot-1), fresh Opus seat; landed by the wave23-design top session (3ddbe853). Ratifications: W2-D2 on the bus.

## What landed

`crates/manifold-core/src/effects.rs` (5,594 lines, five interleaved type families + hand-written serde) is now `effects/{mod,param_defs,bindings,relight,instance,instance_serde,group,driver,envelope,automation,test_support}.rs`; mod.rs is a 104-line facade. Every external `manifold_core::effects::X` path resolves unchanged, including the `ParamId` re-export and the two serde string-path hook targets (`deserialize_opt_generator_instance` from layer.rs:129; `curve_is_linear` from effect_graph_def.rs:473 — both pinned in mod.rs and compile-proved). Zero serde attribute edits; zero signature changes; `PresetInstance` impls moved whole per D4.

Pure-move code commits (all residue-0; → `.git-blame-ignore-revs`):
`b346cb4b` E-S1 git-mv → effects/mod.rs · `72d708b5` E-S2 leaf families (2,020 moved) · `fd722782` E-S3 bindings (676) · `a6912ebf` E-S4 instance_serde (1,493) · `5f5ed191` E-S5 instance (3,159) · `90825e57` E-S6 test distribution (3,678).

## Gate results

- Focused nextest `-p manifold-core -p manifold-editing -p manifold-io`: `728 tests run: 728 passed` — count held at the E-S0 baseline through every slice (worktree includes the LiveSchool fixtures; all `liveschool_*` round-trip tests ran green on every slice).
- Public-item multiset census: 0 baseline tokens missing at every slice; additions only `pub use` facades + `pub(super)` wiring — public API byte-identical.
- `move_identity_check` self-test post-merge: 18/18 (D-19: no other lane touched the verifier).
- Full workspace sweep (build-locked, merged lane): `3850 tests run: 3850 passed (1 leaky), 13 skipped` (22.6s)
- Workspace clippy `-D warnings` + `cargo deny check bans`: clippy `Finished` with -D warnings, zero diagnostics; `bans ok`
- INV-M4 / D-29 oracle (main checkout, post-merge): `cargo test -p manifold-io --test load_project` — `test result: ok. 16 passed; 0 failed` (0.27s) (LiveSchool + burn-v4/v5 + waypoints round-trips; the save→load→save structural self-comparison rides `liveschool_roundtrip_preserves_addressing_sites` / `load_burn_v5_roundtrip_json`).
- Deletion proof: `test -f …/effects.rs` → exit 1; `test -d …/effects` → exit 0.
- Verification level: **L1** (`Demo: none — L1` per design — no observable surface).

## Decisions at execution (ratified W2-D2)

1. Single-consumer serde default fns homed beside their type (unqualified `default = "…"` paths resolve in the deriving module — local homing needs zero cross-module wiring); multi-module defaults stay in mod.rs.
2. PresetInstance tests split by subject: serde/round-trip → instance_serde, inherent-method → instance.

Shortcuts taken: none (both seat-reported and gate-verified).

## For Peter (≤2-minute check)

1. `ls crates/manifold-core/src/effects/` — 11 files, no effects.rs.
2. `wc -l crates/manifold-core/src/effects/mod.rs` — ~104-line facade.
3. Load the LiveSchool project in the app; scrub a param with a driver + an envelope — modulation identical (728 tests incl. every round-trip ran unmodified).

Escapes/verification debt: none opened.
