# Landing report — Wave 2 P2-Z: ceilings + supersession (Wave 2 execution COMPLETE, 2026-07-22)

**Design:** `docs/MODEL_COMMAND_DECOMPOSITION_DESIGN.md` §4 P2-Z. Executed directly by the wave23-design top session (3ddbe853) on `lane/wave2-exec` (semantic close-out — small, one commit `6b54e31b`).

## What landed

- **`crates/manifold-app/tests/godfile_regrowth.rs` — BUILT** (the enforcement Wave 1 D11 specified but never created; Wave 2 review M4). Per-file line ceilings for all 25 Wave-2 modules + 7 open Wave-1 register files (interim, tighten at Wave 1 close). Rides nextest; failure message points at the register; ceilings may not be bumped without a register/design update.
- Register updated: Wave 2 was/now table, crate-pair layer vocabulary (Model/Migration/Query/Command), `service.rs`/`undo.rs` non-target verdicts, enforcement rows corrected.
- Sediment notes: `docs/notes/wave2-sediment-notes.md` — none found; two look-vestigial-but-live items pre-answered.
- Supersession sweep run: docs + memory (`godfile-decomposition-campaign` memory re-pointed at both wave designs).

## Gates

- Full workspace sweep (build-locked, merged lane): `3851 tests run: 3851 passed, 13 skipped` — the +1 is `godfile_regrowth`, green.
- Workspace clippy `-D warnings`: clean; `cargo deny check bans`: `bans ok`.
- INV-M4 oracle (main checkout, post-merge): `test result: ok. 16 passed; 0 failed`.
- INV-M7: `UndoRedoManager::new` count 5 == baseline (no second undo stack appeared during the wave).
- Verification level: L1 (`Demo: none — L1`).

## Wave 2 execution — final state

All four phases landed same-day under D-28/D-29: P2-G (`3d9e4abd`), P2-E (`fce74213`), P2-P (`89e9e261`), P2-Z (this landing). 18,054 lines across three god files → 25 single-concern modules behind stable facades; 21 pure-move code commits, every one residue-0; zero serde/API/behavior change (censuses + oracle + unmodified 728-test corpus prove it). Design decisions remain PROPOSED for Peter's morning review of the record; Wave 3 execution stays fenced (D-28).

Shortcuts taken: none. Escapes/verification debt: none opened.
