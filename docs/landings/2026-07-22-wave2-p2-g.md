# Landing report — Wave 2 P2-G: commands/graph.rs → commands/graph/ (2026-07-22)

**Design:** `docs/MODEL_COMMAND_DECOMPOSITION_DESIGN.md` §4 P2-G (PROPOSED per D-28/D-29 overnight mandate; Peter's morning review pending — this landing is the mechanical pure-move phase those rulings authorize).
**Lane:** `lane/wave2-exec` (slot-1). Executed by an Opus dispatcher seat (S0a–S6) + a fresh Opus seat (S7/S8); landed by the wave23-design top session (harness 3ddbe853).

## What landed

`crates/manifold-editing/src/commands/graph.rs` (9,538 lines, 29 command types, ~50% tests) is now `commands/graph/{mod,test_support,node_edit,expose,groups,scene,modifiers,paste}.rs`. Every external `commands::graph::X` path resolves unchanged through the `mod.rs` facade. Zero signature changes, zero serde exposure (commands never serialize), zero semantic edits.

Pure-move code commits (all `move_identity_check` residue-0; → `.git-blame-ignore-revs`):
`ebdf4999` S1 git-mv → graph/mod.rs · `9f5a0acd` S2 node_edit · `e19ffc29` S3 expose · `966461ab` S4 groups · `522eb2a8` S5 scene · `21844a9b` S6 modifiers+paste · `aac3807c` S7-pre use-header collapse · `6c0a7411` S7 test distribution (9,522 moved lines, residue 0).
Tooling: `08ff497d` S0b — D7a test-mod wiring class in `scripts/move_identity_check.py`, self-test 18/18 (shared gate widened; D-19 checked at landing: P-D did not touch the script, lane version is a superset).

## Gate results (verbatim where it matters)

- Command MULTISET census: 161 raw occurrences / RenameGroupCommand=2 preserved — diff vs S0a baseline EMPTY (the H1 set-census blind spot is exactly what this guards).
- Public-item census: `comm -23 baseline current` → EMPTY (0 baseline items missing); additions are only `pub use`/`pub(super)`/`mod` wiring.
- `cargo nextest run -p manifold-editing`: 252/252, count unchanged through every slice (INV-M3 — undo suite unmodified).
- Full workspace sweep (build-locked, merged lane): `3850 tests run: 3850 passed (1 leaky), 13 skipped` (15.5s)
- `cargo clippy --workspace --tests -- -D warnings` + `cargo deny check bans` (merged lane): clippy `Finished` with -D warnings, zero diagnostics; `bans ok`
- INV-M4 oracle (main checkout, post-merge): `cargo test -p manifold-io --test load_project` — `test result: ok. 16 passed; 0 failed` (0.29s) (includes `liveschool_roundtrip_preserves_addressing_sites` + `load_burn_v5_roundtrip_json`, the save→load→save structural self-comparison of D-29).
- Deletion proof: `test -f …/commands/graph.rs` → exit 1; `test -d …/commands/graph` → exit 0.
- Verification level: **L1** (design: pure moves have no observable surface; `Demo: none — L1` per doc).

## Decisions made at execution (ratified W2-D1 on the bus)

1. Code-move commits are pure; queue-tick bookkeeping rides separate `.claude/`-only commits (keeps the exit-code gate meaningful).
2. Multi-module helpers stay `pub(super)` in `graph/mod.rs` (usage-wins-over-listing — the D3 table listed some under groups/paste; call-site counts said otherwise). Fixture placement followed the same rule: `test_support.rs` holds exactly the 7 fixtures used by ≥2 modules; `project_with_one_master_effect` lives in node_edit (all 17 call sites there).
3. S7 split into S7-pre + S7: deleting the flat test-mod's multi-line use header alongside the distribution tripped a use-block-tracker blind spot (opener stolen as a move); collapsing the header first (its own residue-0 commit) made both commits pure instead of shipping residue under D-9.

Shortcuts taken: none (both seats reported none; verified by gates, not self-report).

## For Peter (≤2-minute check)

1. `ls crates/manifold-editing/src/commands/graph/` — 8 files, no graph.rs.
2. `rg -c 'pub use' crates/manifold-editing/src/commands/graph/mod.rs` — the facade.
3. Open the app, edit any effect graph (add node, group, undo) — behavior identical; undo history works (252 undo-suite tests ran unmodified).

Escapes/verification debt: none opened — no observable surface changed; nothing waived.
