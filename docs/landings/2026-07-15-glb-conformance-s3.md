# Landing — GLB_CONFORMANCE session 3: G-P6 + G-P7 (design complete)

**Date:** 2026-07-15 · **Branch:** `wave/glb-conformance-s3` → main `ad25c021` · **Orchestrator:** Fable 5, Sonnet 5 workers (one per phase; G-P6 worker sent back once)

**Status line after landing (quoted verbatim from docs/GLB_CONFORMANCE_DESIGN.md):**
> **Status: SHIPPED · 2026-07-15 · Fable 5 (authored) + Sonnet 5 (G-P1+G-P2 executed and landed same day, `909976d2`; G-P3+G-P4+G-P5 executed and landed same day, session 2; G-P6 executed and landed same day, session 3, `017e1e41`; G-P7 executed and landed same day, session 3). All seven phases SHIPPED: G-P1 (conformance harness), G-P2 (cap deleted, import is 1:1, BUG-163 fixed as a side effect), G-P3 (anisotropic filtering), G-P4 (KHR_texture_transform all five map families + specular/ior F0), G-P5 (clearcoat lobe, factor-only), G-P6 (node.hdri_source, env_mode card switch, Softbox stays default), G-P7 (full 148-asset Khronos manifest classified — 56 expect_pass / 92 xfail / 0 unclassified; certification record `docs/GLB_CONFORMANCE_STATUS.md`; 8 new bugs logged BUG-166–BUG-173 for genuinely-new gaps the full sweep surfaced, none fixed this phase per its own scope).**

## What landed

- **G-P6** (`017e1e41` + gate-review fix `dc4f1606`): `node.hdri_source` — IoBridge primitive shaped on `gltf_texture_source` (background-thread EXR decode → `Rgba16Float` upload → mipmapped blit; EXR is linear, **no color_space param exists** — a unit test asserts its absence). `image` dep gains the `exr` feature (the one approved dependency change). Import graph: `env_mode` card enum (**Softbox = 0 stays the default** — Peter's black-void look is the product look; HDRI is opt-in) driving the existing `node.switch_texture` mux; `hdri_file` Browse binding; `env_intensity` fans out to both softbox intensity and an HDRI-branch `node.exposure` gain, so the one Environment fader works in both modes. Prefilter cost measured: 4.3 ms/frame at the shipped 2048×1024 default; 14.2 ms at 4096×2048 (over the 10 ms trigger — the doc's default stands, verified not assumed).
- **G-P6 gate-review find, fixed at the root** (`dc4f1606`): the orchestrator rejected the first demo — the "HDRI-lit" helmet was near-black because `render-import` declared byte-stable convergence before the 74 MB EXR's background decode landed. No pending-IO surface existed anywhere (verified by search), so the worker added the minimal one: `EffectNode::io_pending()` (defaulted `false`) surfaced through `PresetRuntime::io_pending()`; byte-stable frames don't count while IO is pending. `gltf_texture_source` now reports through it too, closing the same latent race for slow glb textures. **Deviation from the brief, flagged:** this is a new (defaulted) trait method the doc didn't specify — added under orchestrator direction because the doc's own audit row requires the harness to "convergence-poll background decodes" and no mechanism existed.
- **G-P7** (`37c81fba`): manifest grown to the **full pinned Khronos suite: 148 entries, 0 unclassified** — 56 `expect_pass` (each with numeric checks + a 2/255 self-golden), 92 `xfail` (49 `deferred-3` extension families, 29 no-glTF-Binary-variant-at-pin, 8 newly logged bugs BUG-166–BUG-173, 2 BUG-165 never-converges, 1 BUG-164 sampler settings), 12 distinct reasons, every one a named deferred item, phase, or BUG-NNN. Certification record: `docs/GLB_CONFORMANCE_STATUS.md`, generated by `scripts/gen_glb_conformance_status.py` from the manifest (single source of truth; do-not-hand-edit header). BUG-163 was already FIXED in the backlog (session 2) — untouched.

**The xfail count, as the G-P7 gate requires it reported: 92** (of 148; 56 expect_pass, 0 unclassified).

## Gate output (verbatim, warm main checkout, post-merge)

```
cargo clippy --workspace -- -D warnings              → Finished (clean)
cargo deny check bans                                → bans ok
cargo nextest run --workspace                        → 3398 tests run: 3398 passed, 12 skipped
cargo run -p manifold-renderer --bin check-presets   → 57 presets: 57 ok, 0 failed
cargo test ... --test gpu_proofs -- --test-threads=1 → 50 passed; 0 failed
cargo test ... --test glb_conformance -- --test-threads=1 →
  glb conformance summary: 56 expect_pass checked, 92 xfail, 0 skipped (not fetched), 0 failures
  (92.84s; full suite fetched in main via scripts/fetch-gltf-conformance.sh — 111 fetched, 15 present)
```

G-P6 negative gates rerun by the orchestrator in the worktree: content-thread grep (decode only inside `thread::spawn`), no `color_space` anywhere in the primitive, §4's exact tonemap gate on `src/bin/` zero hits. Conformance sweep byte-stable pre/post G-P6 (HDRI is opt-in), check-presets green, `-p manifold-renderer --lib` 1260 passed.

## Verification level reached

**L2** everywhere. Orchestrator independently reran every phase gate, script-verified the manifest (148 total / 0 unclassified / every expect_pass carries a golden check — computed, not eyeballed), and read the artifacts: the near-black first HDRI demo (rejected), the fixed sky-lit renders, and **three held-out suite PNGs from assets never named in any brief — CesiumMilkTruck (livery + glass correct), Lantern (warm emissive glass), WaterBottle (metallic sheen + roughness variation) — all render correctly.** The acceptance demo was rerun in the main checkout post-merge (converged frame 126, non-black 0.1333, PNG read).

## Demo artifacts

- HDRI-lit DamagedHelmet (main checkout, brief's command + `env_intensity=2.5`): scratchpad `helmet_hdri_main.png`; default-exposure and softbox-baseline variants in the same directory.
- Full-suite renders (49 newly-classified assets): scratchpad `conformance_pngs/`.
- The certification table itself: `docs/GLB_CONFORMANCE_STATUS.md`.

## Click-script for Peter (≤2 minutes)

1. `bash scripts/fetch-gltf-conformance.sh` (once; ~118 assets, pinned commit).
2. `cargo run -p manifold-renderer --bin render-import -- tests/fixtures/gltf/DamagedHelmet.glb --param env_mode=1 --param hdri_file=tests/fixtures/gltf/kloppenheim_07_puresky_4k.exr --param env_intensity=2.5 --out /tmp/helmet_sky.png` — expect the helmet lit by real sky, bluish cloud reflections in the visor. Drop `--param env_mode=1` to confirm the softbox default is untouched.
3. Open `docs/GLB_CONFORMANCE_STATUS.md` — the certification table: 148 assets, 56 pass, 92 xfail, every xfail named. This is now the honest surface of "drop in any glb".
4. In the app: import a glb, open the import card's Environment section — `env_mode` Softbox/HDRI enum + HDRI file Browse; ride the same Environment fader in either mode.

## Deviations from the briefs

- `EffectNode::io_pending()` (above) — new defaulted trait method, orchestrator-approved, doc didn't specify it.
- `env_intensity` fan-out to the HDRI branch — the worker's first pass left HDRI mode with no exposure control (confessed as a shortcut); the fix reuses the existing `node.exposure` atom and the established fan-out pattern. Measured basis: kloppenheim mean radiance 0.24 (~4× dimmer than the softbox default), so an exposure control is load-bearing, not polish.
- G-P7's 29 no-glb-variant assets are `xfail:G-P7` (per the orchestrator's clarification following the TextureTransformTest precedent) rather than pointing at a §7 deferred item — the reason is visible and counted; converting them means teaching the fetch script gltf+sidecar handling per asset (BUG-166–BUG-173 hunting may absorb some).
- Worker "released" the slot mid-session and a second worktree (slot-3) ended up holding the branch; the orchestrator consolidated onto slot-3, neutralized slot-2 (synced + detached), and verified no work was lost (slot-2's staged diff was exactly the reverse of the committed fix — phantom staging from the branch ref advancing under it).

## Notes / carried debt

- Pre-existing, untouched: two ad-hoc tonemaps in `tests/` (`fluid3d_bias.rs:162`, `gpu_proofs/film_grain_decorrelation.rs:251`) predate this design; §4's gate is scoped to `src/bin/` and passes. Candidates for a later `headless_readback` migration.
- `docs/BUG_BACKLOG.md` has **two entries named BUG-163** (freeze-codegen, line ~879, FIXED; amg-livery, line ~2328, FIXED) — a pre-existing ID collision worth a one-line dedup pass.
- BUG-165 (BoomBox/VirtualCity never-converge) and BUG-166–BUG-173 root causes logged as "not investigated" — classification-depth only, per G-P7's scope.
- The G-P6 first-demo escape (converged-before-decode) is exactly the class the io_pending surface now deletes; no VERIFICATION_DEBT entry needed — the mechanism is the fix.
