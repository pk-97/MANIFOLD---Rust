# Landing report — Wave 3 batch 3 (P3-Z close-out) — WAVE 3 COMPLETE

**Landed:** 2026-07-22, lane/wave3-batch2 → main (claim on the bus, wave3 top session).

## What landed

- `godfile_regrowth` ceilings for every new Wave-3 module (1554b1c5) — current+10% rounded to 50s; the two sanctioned exceptions pinned tighter: `preset_runtime/core.rs` 2200 (Peter-sanctioned ~2k), `codegen/fused.rs` 1600. Test green.
- Register update (1ba353aa): Wave-3 rows → landed directories; D7 house rules (tests in-file until ~500 lines then sibling file; ~1.5k code target with named ceilings) recorded as campaign policy; ROW_MODEL_EDGES annotated with D9's context-struct precedent.
- FREEZE_COMPILER_MAP size refresh (0d13b24d, INV-R7).
- Sediment notes (2b19f920): the W3-D1 #[path] pattern, the W3-D2/D3/D4 verifier classes, residue-0 ≠ compiles (W3-D5).
- Supersession sweep: zero stale doc hits (all matches are dated historical records or accurate current text); 8 memory files' single-file paths updated to directory forms by the top session.
- Status flip: design doc → COMPLETE (c8af23ba).

## The wave's single gpu-proofs execution (D-45)

`cargo test -p manifold-renderer --features gpu-proofs` → **1827 passed; 2 failed; 23 ignored** (91.72s).

The 2 failures are **PRE-EXISTING, confirmed by observation**: a controlled run of exactly those tests on pre-Wave-3 code (eed0eaeb; fixtures md5-verified identical; same machine/session) fails identically — `skinned_characters_render_four_visibly_distinct_deformed_poses` (Fox/CesiumMan render ALL-BLACK — blank headless render, not a skinning no-op) and `bug221_layout_preserved_before_and_after_fix` (mean_abs_diff 0.0157 over threshold). Wave 3 only pure-moved these tests; its behavior gates (byte-identical WGSL, byte-identical defs across 145 fixtures) were all green. Logged: **BUG-304** (the failures; first suspect is the headless GPU environment — the machine hard-crashed and rebooted mid-campaign; next step is an in-app L2 look) and **BUG-305** (the run exposed that gpu render tests OVERWRITE tracked golden PNGs in place — 24 dirtied goldens were reset in both worktrees, never committed; goldens must become read-only inputs).

D-45's residual-risk clause (design §2 consequence 5) named exactly this outcome: a GPU-only issue surfaces at the single P3-Z run. It did — and it predates the wave.

## Wave 3 totals

Three god-files (23,051 lines) → 27 focused modules + 13 test files; two function-per-feature catalogs → typed const tables (adding a KHR texture family = one row); every >6-param production signature in the three trees fixed or KEEP-ruled; zero production `too_many_arguments` allows remain. Zero behavior change, proven at three layers (move-identity residue, WGSL byte snapshots, def-JSON equivalence incl. held-out fixtures).

## Level reached

L1 across the wave (the target for behavior-preserving work). Verification debt: BUG-304's in-app look is the one open observation — logged as the bug's next step, not carried silently.

## Click-script for Peter (≤2 min)

1. Open the app, import a .glb — identical result (defs byte-identical to pre-wave).
2. **Look at a skinned import (Fox/CesiumMan) playing — if it's black or frozen in the APP, BUG-304 is real and MED-HIGH; if it animates, BUG-304 is a headless-env artifact and LOW.** This is the one thing only your eyes can settle.
