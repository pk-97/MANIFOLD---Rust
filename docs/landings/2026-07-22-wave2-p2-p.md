# Landing report — Wave 2 P2-P: project.rs → project/ (2026-07-22)

**Design:** `docs/MODEL_COMMAND_DECOMPOSITION_DESIGN.md` §2 D5, §4 P2-P (D-28/D-29 overnight mandate; Peter's morning review pending).
**Lane:** `lane/wave2-exec` (slot-1), fresh Opus seat; landed by the wave23-design top session (3ddbe853). Ratifications: W2-D3 on the bus.

## What landed

`crates/manifold-core/src/project.rs` (2,922 lines — load-migration + preset registry + queries + validation in one impl) is now `project/{mod,load_migration,presets,queries,validate,test_support}.rs` (208/1,275/424/792/173/85 lines). `crate::project::X` paths all resolve through the facade (incl. `PurgeResult`/`LoadReport` re-exports). `on_after_deserialize`'s migration call sequence is byte-identical — zero reordering (the load-bearing constraint of this phase).

Pure-move code commits (all residue-0; → `.git-blame-ignore-revs`):
`219c5b28` P-S1 git-mv → project/mod.rs · `78ad47dc` P-S2 load_migration · `2da57354` P-S3 presets+queries · `038ccbf0` P-S4a validate · `002b09e3` P-S4b test distribution.

## Gate results

- Focused nextest `-p manifold-core -p manifold-editing -p manifold-io`: `728 tests run: 728 passed` — count held from P-S0 through every slice.
- Public-item multiset census: 0 baseline items missing; sole addition `pub use validate` wiring.
- `git diff --name-only` over the phase: only `crates/manifold-core/src/project/*` — zero import-site edits anywhere else (INV-M5).
- Whole-range move_identity (`219c5b28^..HEAD`): PURE MOVE PROVEN; 5 net visibility widenings, all named (1 runtime cross-sibling `pub(super)`, 4 test fixtures) — the 4 migration methods widened mid-phase were narrowed back to private at P-S4b (baseline-private end state).
- Full workspace sweep (build-locked, merged lane): `3850 tests run: 3850 passed, 13 skipped` (27.3s)
- Workspace clippy `-D warnings` + `cargo deny check bans`: clippy `Finished` with -D warnings, zero diagnostics; `bans ok`
- INV-M4 / D-29 oracle (main checkout, post-merge): `cargo test -p manifold-io --test load_project` → `test result: ok. 16 passed; 0 failed` (0.27s)
- Deletion proof: `test -f …/project.rs` → exit 1; `test -d …/project` → exit 0.
- Verification level: **L1** (`Demo: none — L1` per design).

Shortcuts taken: none (seat-reported, gate-verified). Parks: none — no load-migration need surfaced (D-29 untriggered).

## For Peter (≤2-minute check)

1. `ls crates/manifold-core/src/project/` — 6 files, no project.rs.
2. `rg -n 'fn on_after_deserialize' crates/manifold-core/src/project/load_migration.rs` — the migration home.
3. Open any old project file in the app — loads identically (LiveSchool + burn-v4/v5 + waypoints round-trips green).

Escapes/verification debt: none opened. Wave 2 remaining: P2-Z (ceilings + supersession — builds `godfile_regrowth`).
