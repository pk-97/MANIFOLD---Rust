# WS1 P-B slice queue — overnight run 2026-07-21

Dispatcher pops top-to-bottom; S1/S2 may swap freely; S3–S7 require S2 landed (helper paths). One Sonnet lane per slice, ONE commit, in slot-3 on `lane/ws1-bridge`. Gates per slice (all exit codes, quote outputs in the commit message): `python3 scripts/move_identity_check.py <commit>` · census `rg -o --no-filename 'PanelAction::[A-Za-z0-9]+' crates/manifold-app/src/ui_bridge/dispatch/ crates/manifold-app/src/ui_bridge/inspector.rs | sort -u | wc -l` == **150** (D-20 ii: `--no-filename` is REQUIRED — without it rg prefixes `path:` on multi-file searches and `sort -u` double-counts variants referenced in BOTH a dispatch module and inspector.rs test fixtures; the un-prefixed form gave a false 173) · `cargo clippy -p manifold-app --tests -- -D warnings` · `cargo nextest run -p manifold-app` (includes `dispatch_chain_completeness` + the three oracle suites). Any friction not covered by `decisions.md` → PARK (append to `parked.md`, skip slice, continue).

The proven recipe (browser slice, twice-validated) — per domain:
1. Move the domain's arms + DOMAIN-ONLY helpers VERBATIM to `ui_bridge/dispatch/<d>.rs`:
   `pub(crate) fn dispatch_<d>(action: &PanelAction, ctx: &mut super::super::DispatchCtx) -> DispatchResult { match action { <arms>, _ => DispatchResult::unhandled() } }`
2. In `dispatch_inspector`: `let r = super::dispatch::<d>::dispatch_<d>(action, ctx); if !r.unhandled { return r; }` · in `dispatch/mod.rs`: `pub(crate) mod <d>;`
3. Run the four gates.

- [x] **S1 clip** — arms inspector.rs:753–930 + `apply_detection_edit` (clip-only) + its imports. Preamble-free. READY. — DONE 4d271d97 (G1 pure move residue 0 scaffold 5/25; census 150; clippy 0; nextest 319/319 incl. chain-completeness). Accepted, gates re-run independently.
- [x] **S2 resolve** — pure helper move: dual-edit helpers (:61–223), resolvers (:223–341), `resolve_param_range`, `preset_source_def`, `audio_setup_command` → `dispatch/resolve.rs` (pub(crate)); 86 call sites keep working via use-wiring/path updates (wiring class in the verifier). No chain call, no census change; chain-completeness exempts resolve.rs already. — DONE 1f5c9247 (UNAMENDED; G1 initially residue 4 = use-block continuation lines, a verifier class gap → fixed in S2b; post-fix G1 PURE MOVE PROVEN residue 0, 11 visibility pairs; census 150; clippy 0; nextest green). Accepted after S2b.
- [x] **S2b verifier class fix (D-18)** — move_identity_check.py stateful use-block tracking (multi-line `use{…}` continuation lines are wiring, smuggle-proof) + 2 self-test fixtures. — DONE 4b3a6bbb (self-test 7/7 incl. both new D-18 cases; G1 on 1f5c9247 → residue 0). Own tooling commit per D-7. Shared gate per D-19 (2nd lander merges the D-15 wrapper class + fixtures).
- [x] **S3 params** — preamble domain (D-11). — DONE (chain: tooling 020792a8 = D-11 preamble-as-scaffold; move c1740f2f = pure move residue 0, scaffold 7/25 [chain-head + 2 preamble lines], census 150, clippy 0, nextest green). 63 params arms moved. Accepted after S3b+S3c restored gate integrity; c1740f2f re-proven residue 0.
- [x] **S3b verifier fix (D-20 i+iii)** — context-opener use-block tracking + drifted-preamble removal entries. — DONE c98d3a14 (self-test 13/13). Its per-line drifted entries were superseded by S3c (D-21); context-opener fix stands.
- [x] **S3c verifier fix (D-21)** — replaced per-line drifted-preamble entries (short generics like `");"` masked genuine deletions) with a stateful removal-side SEQUENCE matcher. — DONE e61edd94 (self-test 14/14; lone-`");"` deletion now CAUGHT; in-order drifted removal still PROVEN; c1740f2f still pure). Gate integrity restored.
- [x] **S4 modulation** — DONE 4bad469e (G1 pure move residue 0, scaffold 7/25; census 150; clippy 0; nextest 319/319 incl. chain-completeness; dead `InspectorTab` import dropped as wiring in the same amended commit). 26 arms moved. — preamble domain (D-11); heaviest helper user. PARTITION (26, from S3 lane, verified vs inspector.rs body; under the "Effect modulation" + "Modulation undo: snapshot/commit" comment blocks): DriverToggle, AudioModToggle, AudioModSetSource, AudioModRemove, AudioModSetInvert, AudioModSetRateOfChange, AudioModShapeSnapshot, AudioModShapeParamChanged, AudioModShapeCommit, AudioModSetTriggerMode, AudioModSetActionKind, AudioModStepAmountSnapshot, AudioModStepAmountChanged, AudioModStepAmountCommit, AudioModSetWrap, EnvelopeToggle, DriverConfig, TrimChanged, TargetChanged, EnvDecayChanged, TrimSnapshot, TrimCommit, TargetSnapshot, TargetCommit, EnvDecaySnapshot, EnvDecayCommit. Byte-exact D-11 preamble atop dispatch_modulation. (Verifier now handles mid-block use-list removal under an unchanged opener — D-20 i / D-21 — so that S3 friction won't recur.)
- [x] **S5 mapping** — DONE fb59db17 (UNAMENDED; G1 pure move residue 0, scaffold 13/25 incl. added canonical preamble + the retired DRIFTED original preamble via the D-21 sequence matcher; census 150; clippy 0; nextest green). 14 arms moved; as the LAST preamble domain it removed the now-dead drifted original. Accepted after S5b fixed a verifier moved-flag desync (is_moved short-circuit skipped the tracker advance). — preamble domain (D-11). PARTITION (14, under "Macro mapping" + "Ableton mapping" blocks, tail of match after ParamLabelRightClick): MapParamToMacro, MacroLabelRightClick, UnmapMacro, ClearMacroMappings, MapParamToAbleton, UnmapParamAbleton, MapMacroToAbleton, UnmapMacroAbleton, OpenAbletonPickerForMacro, AbletonMacroTrimChanged, AbletonMacroTrimSnapshot, AbletonMacroTrimCommit, AbletonInvertToggle, AbletonMacroInvertToggle. Byte-exact D-11 preamble atop dispatch_mapping.
- [x] **S5b verifier fix (moved-flag desync)** — advance the D-21 drifted-sequence tracker BEFORE the `is_moved` short-circuit (git content-matching a drifted `);` as moved-elsewhere skipped the tracker advance → desync → residue). — DONE a3418c9b (self-test 15/15; new moved-flagged-collision case PROVEN, out-of-sequence still CAUGHT, in-order still PROVEN; fb59db17 → residue 0). Within D-21 intent, no new decision. Lesson (carry to S9): synthetic fixtures can pass a broken impl — the real-commit bonus gate caught it; bonus-gate every verifier slice against the real commit.
- [x] **S6 audio_setup** — DONE 5c1cb982 (plain one-liner message kept — gate outputs recorded here, NOT via a non-HEAD amend). 24 arms moved, no preamble (correctly). Fixes folded in: dead `AudioShapeParam` + `ContentCommand` imports dropped, doc-continuation fixed, and the now-emptied `dispatch_inspector` match collapsed to the bare `DispatchResult::unhandled()` router terminal. GATES (verifier @ cbf225ba/S6b): G1 genuine PURE MOVE PROVEN residue 0, scaffold 4/25; G2 census 150; G3 clippy exit 0; G4 nextest 319/319 incl. chain-completeness. dispatch_inspector is now ROUTER-ONLY (chain calls + bare unhandled tail, no arms). — NOT a preamble domain
- [x] **S6b verifier fix (bare router terminal)** — extend SCAFFOLD pattern 3 to also match a bare `DispatchResult::unhandled()` router tail (semantics-free null action; honest classification, not gate-bending — avoids pre-accepted-red, keeps exit-code discipline S9 needs). — DONE cbf225ba (self-test 16/16, new router-collapse PROVEN; bonus-gate: 5c1cb982 residue 1→0). Option-(a) ruling; supersedes the crossed pre-accepted-red ruling. (no D-11 preamble unless a moved arm references effective_tab/active_layer — verify per-arm; if one does, STOP/report, likely misclassification). PARTITION (24, incl. layer clip-trigger arms tied to AUDIO_SETUP_DOCK_AND_TRIGGER_UNIFICATION_DESIGN.md + the "Audio Setup (project-level send routing)" block): AudioTriggerSectionToggle, AudioTriggerRowExpandToggle, AudioTriggerAdd, AudioTriggerRemove, AudioTriggerEnabledToggle, AudioTriggerSetSource, AudioTriggerShapeSnapshot, AudioTriggerShapeParamChanged, AudioTriggerShapeCommit, AudioTriggerSetLength, AudioSetDevice, AudioAddSend, AudioRemoveSend, AudioRenameSend, AudioSetSendChannels, AudioSendGainStep, AudioSendGainDragBegin, AudioSendGainDragChanged, AudioSendGainDragCommit, AudioSendGainSetTyped, AudioSendFloorStep, AudioCrossoverDragBegin, AudioCrossoverChanged, AudioCrossoverCommit.
- [x] ~~**S7 scene**~~ — STRUCK (D-22 ws1): zero scene arms is correct-by-design — scene converged onto the card/params path (SCENE_PANEL_EXPOSURE_CONVERGENCE end-state). Module set has NO scene.rs (params/modulation/mapping/audio_setup/browser/clip/resolve). After S5+S6 → straight to S9.
- [x] ~~**S8 flow-manifest rider**~~ — STRUCK: reassigned from WS1 to the WS2 seat per D-20 (scripts-only, zero overlap with dispatch slices). Do not start.
- [ ] **S9 phase close** — verify `dispatch_inspector` is router-only; full-lane `move_identity_check origin/main..HEAD`; census LIST diff vs entry census == empty (sorted variant names, not just count); report to team-lead for landing.

Landing is the TOP SESSION's job only. Dispatcher never merges to main, never pushes.
