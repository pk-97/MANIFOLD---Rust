# Standing decisions — god-file Wave 1 (append-only; team-lead writes, everyone reads)

Rule for all agents: BEFORE pausing on any fork, re-read this file — if your question is answered here, proceed; if not, PARK it in `parked.md` and continue with the next queue item. Chat messages are for nudges and reports; rulings live here.

- **D-1** Router = ordered first-non-unhandled chain; NO per-variant delegation arms (a hand-written variant→module table is a parallel routing copy — forbidden by name).
- **D-2** `DispatchResult.unhandled: pub(crate) bool`, true only via `unhandled()`; sole consumer is the router chain. Zero struct-literal construction outside the module (grep-gated).
- **D-3** Dispatch body references `ctx.X` directly (approach A). No per-module reborrow preambles; allowlisting bindings is forbidden (a wrong-field bind is a silent state reroute).
- **D-4** Verifier scaffold class: narrow patterns only, cap 25/commit, NEVER any `PanelAction::` pattern. Routing correctness is proven by census equality + chain-completeness, never by allowlist.
- **D-5** Variant-census equality == 150 after every slice; at phase end the sorted variant LIST diff vs entry census must be empty.
- **D-6** `dispatch_chain_completeness` invariant: one chain call per handler module file (mod.rs, resolve.rs exempt); rides nextest.
- **D-7** Pure-move and semantic changes NEVER share a commit. Lane never lands; top session merges behind the full gate.
- **D-8** Removed `#[allow(dead_code)]` lines are legitimate residue when the allow's named un-suppression trigger fired — name it in the commit message (precedent: browser slice, sentinel allow).
- **D-9** A slice's tiny (<3-line) genuine moves that fall below git's move threshold: name them in the commit message; reviewer eyeballs at landing. Keep that surface minimal.
- **D-10** Order within P-B: S2 (resolve.rs) lands before any preamble domain (S3–S5) because the helpers are shared (86 call sites across domains).
- **D-11** Preamble domains (params/modulation/mapping): the ONLY sanctioned preamble is byte-exact:
  `let (effective_tab, effective_active_layer) = super::editor_dispatch_context(ctx.editor_target, &*ctx.project, ctx.ui.inspector.last_effect_tab(), ctx.active_layer);`
  `let active_layer = &effective_active_layer;`
  The first such slice adds these two lines as EXACT-STRING (whitespace-normalized) scaffold patterns to `move_identity_check.py` + one self-test fixture, its own tooling commit. Any deviation from the byte-exact form = residue = investigate, never adapt.
- **D-12** Overnight scope fence: P-B remainder (this queue) and, if it completes AND the top session has landed it, P-F pure-move slices only. P-D/P-I/P-S do NOT start overnight.
- **D-13** Command hygiene for auto-approval (Peter 2026-07-21): phrase commands so the auto-mode classifier reads them as non-destructive — plain single-purpose commands; no `$()` writes, no redirects into repo paths, no destructive-git shapes; `/tmp` and `/dev/null` redirects are fine. A blocked command = park the slice, don't retry variants.
- **D-14** Every full workspace build/test/flow run goes through `.claude/scripts/with-build-lock.sh` — one heavy build machine-wide, no exceptions (two GUI lockups on 2026-07-21 correlate with concurrent sweeps).
- **D-15** (top session, WS2 P-F2a) Bare inherent-impl wrapper lines (`impl Type {` + bare closing brace) are ALLOW-class wiring in `move_identity_check.py` — an impl-block move into a submodule needs exactly one; wrappers alone carry no behavior (bodies still classify as moves/residue). Trait impls (`impl X for Y`) stay residue — escalate/park if genuinely needed. Verifier change ships with a 6th self-test case (wrapper move PROVEN; body edit inside wrapper CAUGHT), own tooling commit, flagged as widening the shared gate.
- **D-16** (top session) WS2 scope fence stands: ui_root.rs only until P-B lands on main; then P-F1/app.rs slices re-brief against the landed DispatchCtx call sites.
- **D-17** Orchestration-file topology (fixes the queue-location snag): the dispatcher merges `origin/main` into the lane ONCE (standard pre-landing merge, brings the kit), then OWNS `ws1-queue.md` on the LANE — checkbox commits ride the lane (pathspec), restart-proof, no top-session dependency; the top session freezes its main-side queue copy (lane version wins at landing). `decisions.md` stays MAIN-only, top-session-written; the dispatcher reads it FRESH from the main checkout absolute path (/Users/peterkiemann/MANIFOLD - Rust/.claude/orchestration/decisions.md) before pausing on any fork — never a lane copy. `parked.md` is created and committed on the LANE (seat-owned state).
- **D-18** (S2 review) Multi-line `use` brace-list continuation lines are WIRING: the verifier's ALLOW regex only matched lines that themselves start with `use`, so inner list lines (`  Foo,` / `  Bar};`) and edits removing a now-dead name from a pre-existing multi-line list surfaced as residue (S2, 1f5c9247 — residue 4, all imports, zero body bytes changed). Fix the CLASS in `move_identity_check.py`: stateful use-block tracking (a +/- line matching `use .*\{` with no closing `};` opens a block; subsequent same-sign lines allow ONLY if they are shaped like import items (`ident/path, optional `as` alias, optional closing `};`) — any non-import-shaped line inside the block is residue (smuggle-proof). Ships with two new self-test fixtures: multi-line use move → PROVEN; a code line smuggled inside a use block → CAUGHT. Own tooling commit on the lane (S2b). S2 is then ACCEPTED by rerunning G1 on the UNAMENDED 1f5c9247 → expected residue 0.
- **D-19** `scripts/move_identity_check.py` is a SHARED gate now edited on two live lanes (D-15 impl-wrapper class on lane/ws2-uiroot, D-18 use-block class on lane/ws1-bridge). Whichever lane lands second must merge the other's pattern AND fixtures and rerun the full self-test before its landing gate counts. Top session enforces at landing.
- **D-20** (top session, 20:0x) P-F2a landed on main (`ffbe0df4`): ui_root.rs is now ui_root/{mod,overlay,drag,dropdowns,events}.rs. WS1: merge origin/main into lane/ws1-bridge before the next slice gate (clean — no file overlap). S8 (flow manifest + runner) is REASSIGNED from the WS1 queue to the WS2 seat (scripts-only, zero overlap with dispatch slices) — WS1 dispatcher: strike S8 from your queue, do not start it.
- **D-20** (S3 review — three gate defects, zero code defects; S3 code commit c1740f2f ACCEPTED conditional on S3b): (i) D-18's use-block tracker only arms on a SIGNED opening line — an inner-line edit under an UNCHANGED (context) opener falls to residue; extend tracking to arm on context `use … {` openers too, USE_ITEM shape check unchanged (smuggle-proofing intact). (ii) The census command was defective as written: `rg -o` over multiple paths dedupes on `path:variant`, double-counting variants referenced in both a dispatch module (arms) and inspector.rs (test fixtures). Canonical census command is now `rg -o --no-filename 'PanelAction::[A-Za-z0-9]+' <dispatch dir> <inspector.rs> | sort -u | wc -l` == 150 — dispatcher updates the queue file accordingly. (iii) Pre-empt: inspector.rs's in-source preamble DRIFTED from D-11's canonical form (explicit `&*ctx.active_layer` reborrow + type annotation); when the LAST preamble-using domain moves out, those removed drifted lines will match neither moves nor PREAMBLE_LINES. Add the drifted forms to PREAMBLE_LINES as sanctioned REMOVAL-side entries now. S3b ships (i)+(iii) with one fixture each + the queue command fix; then G1 reruns on the UNAMENDED c1740f2f — expected residue 0 → S3 accepted.
- **D-21** (S3b review) S3b ACCEPTED (13/13, c1740f2f proven residue-0 → S3 ACCEPTED) — but the lane's own friction flag is upheld: registering the drifted preamble as PER-LINE fragments made short generics (`");"`, `"ctx.editor_target,"`, `"&*ctx.project,"`) permanent scaffold entries, which masks genuinely-deleted matching lines in ALL future commits (a dropped arm's call-closers would vanish from its residue signature). S3c micro-slice, BEFORE S4: replace the drifted-preamble per-line entries with a stateful REMOVAL-side sequence match — arm only on the drifted opener line, allow subsequent removal-side lines only while each matches the expected next line of the exact drifted sequence, disarm on completion or any mismatch (mismatch → that line falls to residue). Canonical two-line D-11 entries stay (full distinctive statements). One new fixture: an out-of-sequence `");"` removal stays residue. Self-test 14/14.
- **D-22 (ws1 top session)** Bookkeeping + two rulings: (a) NUMBERING COLLISION — two D-20 entries exist (WS2's P-F2a/S8 entry at cbe18f7b and WS1's S3 ruling at 29a24030); both stand as written; from here every entry is prefixed with its authoring session (ws1/ws2) and takes the next free number checked against the CURRENT file at write time. (b) S7 scene: STRUCK — the 127-arm partition has zero scene arms because scene converged onto the card/params path (SCENE_PANEL_EXPOSURE_CONVERGENCE end-state working as designed); the Wave-1 design doc's D6 module list gets a supersession note at P-B landing (modules: params, modulation, mapping, audio_setup, browser, clip, resolve — no scene.rs). (c) S8 reassignment to WS2 stands, but its artifacts (scripts/run_ui_flows.py + scripts/ui-flows/manifest.json) are NOT yet on main — the P-B landing does NOT block on them: fallback flow gate = the documented-mapping subset (17 P-P flows + 3 P-F2a spot-checks) with the gap named in the landing report, burned down when WS2 delivers.
- **D-23** (top session 012PAn, deconfliction) slot-3 + lane/ws1-bridge are owned EXCLUSIVELY by the 01BALc8 dispatcher loop until P-B lands — confirmed by commit trailers (S5b/S6b) and clean tree after 37015483. The ws1-projection tmux seat is STOOD DOWN from P-B with thanks (its P-P + early P-B architecture landed; ctx 448k = rotation ceiling). The test_move_identity_check.py add/add suite divergence (P-B case_* vs main's WS2 run_case) is assigned to S9 phase close per D-19's second-lander rule — unify BOTH suites, keep every fixture from each. Landing of P-B remains the 012PAn top session's job; S9-reported = signal to land.
- **D-24** (top session 012PAn) P-B LANDED and pushed (see landing report docs/landings/2026-07-21-ui-funnel-p-b.md). Landing lock lifted; slot-3 may be released after ancestor check. P-D remains daytime-fenced (D-12).
- **D-25 (ws1 orchestrator — IDENTITY + YIELD, read before touching slot-3)** Identifier clarification: harness id 01BALc8 and commit-trailer id 012PAn are the SAME session (the ws1 orchestrator writing this). At 22:24 the OTHER top session began executing the P-B landing in slot-3 (merge 65529cc4 + staged .git-blame-ignore-revs) while this session was starting the same landing — near-collision on a shared index. RESOLUTION, effective immediately: the session currently mid-landing OWNS the landing execution to completion (merge, full gate, flows via run_ui_flows.py, landing report, status flip, push). The ws1 orchestrator is HANDS-OFF slot-3 and will act as INDEPENDENT POST-PUSH VERIFIER: re-run the full gate + flow suite + census list-diff against pushed main and publish a verification addendum. Two-lander prevention rule for the rest of the campaign: before ANY landing, the lander appends a LANDING-CLAIM entry here naming its harness id AND waits one minute for a conflicting claim; no claim = no landing.
- **D-25** (top session 012PAn) P-F1 (app_render frame-stage split, design D7 — pure moves, D-12-permitted overnight) is CLAIMED for the ws2-uiroot seat on a fresh slot + lane/ws2-frame. 01BALc8 loop: do not double-staff; your queue is exhausted — stand down or watch parked.md. P-D/P-I/P-S wait for daytime.
- **D-26 (ws1 orchestrator, harness id 01BALc8) LANDING-CLAIM per D-25:** the P-B landing prepared on lane/ws1-bridge (docs commit 0fbc8ec1) has sat un-merged/un-pushed for 40+ minutes while the preparing session moved to other work (raytracing doc, P-F1 claim). Claiming COMPLETION of the P-B landing now: merge --no-ff to main, full gate, flows, census, push. If the prior lander is still mid-ceremony it must say so on this bus within the claim minute.
- **D-27 (ws1 orchestrator) D-26 RETRACTED — my error:** the P-B landing was NOT stalled; it completed at 10a12c1e + addendum 97a39ea6, 5 minutes after the D-24 lock, and my `git log -3` window simply didn't reach below the raytracing commits. Nothing was double-landed (the "completion" merge fast-forwarded onto already-landed content, zero new commits). Proceeding as post-push INDEPENDENT VERIFIER per D-25: full gate + full flow suite via scripts/run_ui_flows.py + census list-diff against pushed main; addendum to follow in the P-B landing report. Lesson for the protocol: a landing-status check is `git merge-base --is-ancestor <lane-tip> origin/main`, never a bounded `git log -N` eyeball.
- **D-28** (top session 012PAn, P-F1 park rulings) (1) P-F1-frame-body: park CONFIRMED — D7's "pure move" phrasing was wrong for tick_and_render's inline stages (design-doc miss, mine); the stage extraction becomes daytime phase P-F1b (semantic), parity oracles = flow suite + INV-G4 trace, shape decided in its brief (top-session recommendation: FrameCtx struct carrying cross-segment locals, mirroring DispatchCtx — but the brief decides). (2) P-F1-drag-structs: DO NOT MOVE, park closes — Bound/UnboundNodeParamDrag are P-I deletion targets (design D4: they die into ScrubState); relocating them to editor_bridge is churn P-I deletes. They stay in app_render.rs until the scrub wire kills them. Neither verifier extension (smuggle-loose) nor D-9 residue needed.
- **LANDING-CLAIM** (top session 012PAn): P-F1 LANDED (sweep 3852/3852, clippy/deny clean, flows 33/33+7 xfail, 40/40 accounted). Claim closed.
- **D-28 (ws1 orchestrator, per Peter 2026-07-22)** wave23-design session mandate EXTENDED: after each design lands PROPOSED + adversarially reviewed, it MAY start Wave 2 execution autonomously using the exact Wave-1 pattern (own Opus dispatcher seat, Sonnet lanes, slice queue file, decisions-bus rulings, slot-ring worktrees, exit-code gates incl. move_identity + a Wave-2 census equivalent, landing-claim protocol, build lock) — SCOPE LIMIT: mechanical pure-move phases ONLY; any phase touching serialized names, load-migration, or undo semantics is FENCED until Peter reviews the design (his morning read). Wave 3 execution does not start overnight regardless. Concurrency: Wave 2 code = manifold-editing/manifold-core; Wave 1 P-D = manifold-ui/manifold-app — low overlap, but ALL landings go through the claim protocol and the build lock serializes gates.
- **D-29 (ws1 orchestrator, Peter 2026-07-22 "Yes")** D-28's serialization/undo fence is LIFTED — full Wave 2 execution may run overnight. Substitute mechanical gate, mandatory per landing that touches serialized names or undo semantics: LiveSchool canonical fixture round-trip green PLUS a save→load→save structural self-comparison (project_tool; byte-stable or field-diff-empty), quoted in the landing report. Any discovered need for load-migration = PARK that phase (never improvise a migration overnight). Peter reviews the PROPOSED designs in the morning as planned; his review no longer blocks execution.
