# Standing decisions — god-file Wave 1 (append-only; team-lead writes, everyone reads)

Rule for all agents: BEFORE pausing on any fork, re-read this file — if your question is answered here, proceed; if not, PARK it in `parked.md` and continue with the next queue item. Chat messages are for nudges and reports; rulings live here.

- **D-1** Router = ordered first-non-unhandled chain; NO per-variant delegation arms (a hand-written variant→module table is a parallel routing copy — forbidden by name).
- **D-2** `DispatchResult.unhandled: pub(crate) bool`, true only via `unhandled()`; sole consumer is the router chain. Zero struct-literal construction outside the module (grep-gated).
- **D-3** Dispatch body references `ctx.X` directly (approach A). No per-module reborrow preambles; allowlisting bindings is forbidden (a wrong-field bind is a silent state reroute).
- **D-4** Verifier scaffold class: narrow patterns only, cap 25/commit, NEVER any `PanelAction::` pattern. Routing correctness is proven by census equality + chain-completeness, never by allowlist.
- **D-5** Variant-census equality == 150 after every slice; at phase end the sorted variant LIST diff vs entry census must be empty.
- **D-6** `dispatch_chain_completeness` invariant: one chain call per handler module file (mod.rs, resolve.rs exempt); rides nextest.
- **D-7** Pure-move and semantic changes NEVER share a commit. Lane never lands; top session merges behind the full gate.
- **D-8** Removed `#[allow(dead_code)]` lines are legitimate residue when the allow's named un-suppression trigger fired — name it in the commit message (precedent: browser slice, sentinel allow).
- **D-9** A slice's tiny (<3-line) genuine moves that fall below git's move threshold: name them in the commit message; reviewer eyeballs at landing. Keep that surface minimal.
- **D-10** Order within P-B: S2 (resolve.rs) lands before any preamble domain (S3–S5) because the helpers are shared (86 call sites across domains).
- **D-11** Preamble domains (params/modulation/mapping): the ONLY sanctioned preamble is byte-exact:
  `let (effective_tab, effective_active_layer) = super::editor_dispatch_context(ctx.editor_target, &*ctx.project, ctx.ui.inspector.last_effect_tab(), ctx.active_layer);`
  `let active_layer = &effective_active_layer;`
  The first such slice adds these two lines as EXACT-STRING (whitespace-normalized) scaffold patterns to `move_identity_check.py` + one self-test fixture, its own tooling commit. Any deviation from the byte-exact form = residue = investigate, never adapt.
- **D-12** Overnight scope fence: P-B remainder (this queue) and, if it completes AND the top session has landed it, P-F pure-move slices only. P-D/P-I/P-S do NOT start overnight.
- **D-13** Command hygiene for auto-approval (Peter 2026-07-21): phrase commands so the auto-mode classifier reads them as non-destructive — plain single-purpose commands; no `$()` writes, no redirects into repo paths, no destructive-git shapes; `/tmp` and `/dev/null` redirects are fine. A blocked command = park the slice, don't retry variants.
- **D-14** Every full workspace build/test/flow run goes through `.claude/scripts/with-build-lock.sh` — one heavy build machine-wide, no exceptions (two GUI lockups on 2026-07-21 correlate with concurrent sweeps).
- **D-15** (top session, WS2 P-F2a) Bare inherent-impl wrapper lines (`impl Type {` + bare closing brace) are ALLOW-class wiring in `move_identity_check.py` — an impl-block move into a submodule needs exactly one; wrappers alone carry no behavior (bodies still classify as moves/residue). Trait impls (`impl X for Y`) stay residue — escalate/park if genuinely needed. Verifier change ships with a 6th self-test case (wrapper move PROVEN; body edit inside wrapper CAUGHT), own tooling commit, flagged as widening the shared gate.
- **D-16** (top session) WS2 scope fence stands: ui_root.rs only until P-B lands on main; then P-F1/app.rs slices re-brief against the landed DispatchCtx call sites.
- **D-17** Orchestration-file topology (fixes the queue-location snag): the dispatcher merges `origin/main` into the lane ONCE (standard pre-landing merge, brings the kit), then OWNS `ws1-queue.md` on the LANE — checkbox commits ride the lane (pathspec), restart-proof, no top-session dependency; the top session freezes its main-side queue copy (lane version wins at landing). `decisions.md` stays MAIN-only, top-session-written; the dispatcher reads it FRESH from the main checkout absolute path (/Users/peterkiemann/MANIFOLD - Rust/.claude/orchestration/decisions.md) before pausing on any fork — never a lane copy. `parked.md` is created and committed on the LANE (seat-owned state).
- **D-18** (S2 review) Multi-line `use` brace-list continuation lines are WIRING: the verifier's ALLOW regex only matched lines that themselves start with `use`, so inner list lines (`  Foo,` / `  Bar};`) and edits removing a now-dead name from a pre-existing multi-line list surfaced as residue (S2, 1f5c9247 — residue 4, all imports, zero body bytes changed). Fix the CLASS in `move_identity_check.py`: stateful use-block tracking (a +/- line matching `use .*\{` with no closing `};` opens a block; subsequent same-sign lines allow ONLY if they are shaped like import items (`ident/path, optional `as` alias, optional closing `};`) — any non-import-shaped line inside the block is residue (smuggle-proof). Ships with two new self-test fixtures: multi-line use move → PROVEN; a code line smuggled inside a use block → CAUGHT. Own tooling commit on the lane (S2b). S2 is then ACCEPTED by rerunning G1 on the UNAMENDED 1f5c9247 → expected residue 0.
- **D-19** `scripts/move_identity_check.py` is a SHARED gate now edited on two live lanes (D-15 impl-wrapper class on lane/ws2-uiroot, D-18 use-block class on lane/ws1-bridge). Whichever lane lands second must merge the other's pattern AND fixtures and rerun the full self-test before its landing gate counts. Top session enforces at landing.
- **D-20** (top session, 20:0x) P-F2a landed on main (`ffbe0df4`): ui_root.rs is now ui_root/{mod,overlay,drag,dropdowns,events}.rs. WS1: merge origin/main into lane/ws1-bridge before the next slice gate (clean — no file overlap). S8 (flow manifest + runner) is REASSIGNED from the WS1 queue to the WS2 seat (scripts-only, zero overlap with dispatch slices) — WS1 dispatcher: strike S8 from your queue, do not start it.
- **D-20** (S3 review — three gate defects, zero code defects; S3 code commit c1740f2f ACCEPTED conditional on S3b): (i) D-18's use-block tracker only arms on a SIGNED opening line — an inner-line edit under an UNCHANGED (context) opener falls to residue; extend tracking to arm on context `use … {` openers too, USE_ITEM shape check unchanged (smuggle-proofing intact). (ii) The census command was defective as written: `rg -o` over multiple paths dedupes on `path:variant`, double-counting variants referenced in both a dispatch module (arms) and inspector.rs (test fixtures). Canonical census command is now `rg -o --no-filename 'PanelAction::[A-Za-z0-9]+' <dispatch dir> <inspector.rs> | sort -u | wc -l` == 150 — dispatcher updates the queue file accordingly. (iii) Pre-empt: inspector.rs's in-source preamble DRIFTED from D-11's canonical form (explicit `&*ctx.active_layer` reborrow + type annotation); when the LAST preamble-using domain moves out, those removed drifted lines will match neither moves nor PREAMBLE_LINES. Add the drifted forms to PREAMBLE_LINES as sanctioned REMOVAL-side entries now. S3b ships (i)+(iii) with one fixture each + the queue command fix; then G1 reruns on the UNAMENDED c1740f2f — expected residue 0 → S3 accepted.
- **D-21** (S3b review) S3b ACCEPTED (13/13, c1740f2f proven residue-0 → S3 ACCEPTED) — but the lane's own friction flag is upheld: registering the drifted preamble as PER-LINE fragments made short generics (`");"`, `"ctx.editor_target,"`, `"&*ctx.project,"`) permanent scaffold entries, which masks genuinely-deleted matching lines in ALL future commits (a dropped arm's call-closers would vanish from its residue signature). S3c micro-slice, BEFORE S4: replace the drifted-preamble per-line entries with a stateful REMOVAL-side sequence match — arm only on the drifted opener line, allow subsequent removal-side lines only while each matches the expected next line of the exact drifted sequence, disarm on completion or any mismatch (mismatch → that line falls to residue). Canonical two-line D-11 entries stay (full distinctive statements). One new fixture: an out-of-sequence `");"` removal stays residue. Self-test 14/14.
- **D-22 (ws1 top session)** Bookkeeping + two rulings: (a) NUMBERING COLLISION — two D-20 entries exist (WS2's P-F2a/S8 entry at cbe18f7b and WS1's S3 ruling at 29a24030); both stand as written; from here every entry is prefixed with its authoring session (ws1/ws2) and takes the next free number checked against the CURRENT file at write time. (b) S7 scene: STRUCK — the 127-arm partition has zero scene arms because scene converged onto the card/params path (SCENE_PANEL_EXPOSURE_CONVERGENCE end-state working as designed); the Wave-1 design doc's D6 module list gets a supersession note at P-B landing (modules: params, modulation, mapping, audio_setup, browser, clip, resolve — no scene.rs). (c) S8 reassignment to WS2 stands, but its artifacts (scripts/run_ui_flows.py + scripts/ui-flows/manifest.json) are NOT yet on main — the P-B landing does NOT block on them: fallback flow gate = the documented-mapping subset (17 P-P flows + 3 P-F2a spot-checks) with the gap named in the landing report, burned down when WS2 delivers.
- **D-23** (top session 012PAn, deconfliction) slot-3 + lane/ws1-bridge are owned EXCLUSIVELY by the 01BALc8 dispatcher loop until P-B lands — confirmed by commit trailers (S5b/S6b) and clean tree after 37015483. The ws1-projection tmux seat is STOOD DOWN from P-B with thanks (its P-P + early P-B architecture landed; ctx 448k = rotation ceiling). The test_move_identity_check.py add/add suite divergence (P-B case_* vs main's WS2 run_case) is assigned to S9 phase close per D-19's second-lander rule — unify BOTH suites, keep every fixture from each. Landing of P-B remains the 012PAn top session's job; S9-reported = signal to land.
- **D-24** (top session 012PAn) P-B LANDED and pushed (see landing report docs/landings/2026-07-21-ui-funnel-p-b.md). Landing lock lifted; slot-3 may be released after ancestor check. P-D remains daytime-fenced (D-12).
- **D-25 (ws1 orchestrator — IDENTITY + YIELD, read before touching slot-3)** Identifier clarification: harness id 01BALc8 and commit-trailer id 012PAn are the SAME session (the ws1 orchestrator writing this). At 22:24 the OTHER top session began executing the P-B landing in slot-3 (merge 65529cc4 + staged .git-blame-ignore-revs) while this session was starting the same landing — near-collision on a shared index. RESOLUTION, effective immediately: the session currently mid-landing OWNS the landing execution to completion (merge, full gate, flows via run_ui_flows.py, landing report, status flip, push). The ws1 orchestrator is HANDS-OFF slot-3 and will act as INDEPENDENT POST-PUSH VERIFIER: re-run the full gate + flow suite + census list-diff against pushed main and publish a verification addendum. Two-lander prevention rule for the rest of the campaign: before ANY landing, the lander appends a LANDING-CLAIM entry here naming its harness id AND waits one minute for a conflicting claim; no claim = no landing.
- **D-25** (top session 012PAn) P-F1 (app_render frame-stage split, design D7 — pure moves, D-12-permitted overnight) is CLAIMED for the ws2-uiroot seat on a fresh slot + lane/ws2-frame. 01BALc8 loop: do not double-staff; your queue is exhausted — stand down or watch parked.md. P-D/P-I/P-S wait for daytime.
- **D-26 (ws1 orchestrator, harness id 01BALc8) LANDING-CLAIM per D-25:** the P-B landing prepared on lane/ws1-bridge (docs commit 0fbc8ec1) has sat un-merged/un-pushed for 40+ minutes while the preparing session moved to other work (raytracing doc, P-F1 claim). Claiming COMPLETION of the P-B landing now: merge --no-ff to main, full gate, flows, census, push. If the prior lander is still mid-ceremony it must say so on this bus within the claim minute.
- **D-27 (ws1 orchestrator) D-26 RETRACTED — my error:** the P-B landing was NOT stalled; it completed at 10a12c1e + addendum 97a39ea6, 5 minutes after the D-24 lock, and my `git log -3` window simply didn't reach below the raytracing commits. Nothing was double-landed (the "completion" merge fast-forwarded onto already-landed content, zero new commits). Proceeding as post-push INDEPENDENT VERIFIER per D-25: full gate + full flow suite via scripts/run_ui_flows.py + census list-diff against pushed main; addendum to follow in the P-B landing report. Lesson for the protocol: a landing-status check is `git merge-base --is-ancestor <lane-tip> origin/main`, never a bounded `git log -N` eyeball.
- **D-28** (top session 012PAn, P-F1 park rulings) (1) P-F1-frame-body: park CONFIRMED — D7's "pure move" phrasing was wrong for tick_and_render's inline stages (design-doc miss, mine); the stage extraction becomes daytime phase P-F1b (semantic), parity oracles = flow suite + INV-G4 trace, shape decided in its brief (top-session recommendation: FrameCtx struct carrying cross-segment locals, mirroring DispatchCtx — but the brief decides). (2) P-F1-drag-structs: DO NOT MOVE, park closes — Bound/UnboundNodeParamDrag are P-I deletion targets (design D4: they die into ScrubState); relocating them to editor_bridge is churn P-I deletes. They stay in app_render.rs until the scrub wire kills them. Neither verifier extension (smuggle-loose) nor D-9 residue needed.
- **LANDING-CLAIM** (top session 012PAn): P-F1 LANDED (sweep 3852/3852, clippy/deny clean, flows 33/33+7 xfail, 40/40 accounted). Claim closed.
- **D-28 (ws1 orchestrator, per Peter 2026-07-22)** wave23-design session mandate EXTENDED: after each design lands PROPOSED + adversarially reviewed, it MAY start Wave 2 execution autonomously using the exact Wave-1 pattern (own Opus dispatcher seat, Sonnet lanes, slice queue file, decisions-bus rulings, slot-ring worktrees, exit-code gates incl. move_identity + a Wave-2 census equivalent, landing-claim protocol, build lock) — SCOPE LIMIT: mechanical pure-move phases ONLY; any phase touching serialized names, load-migration, or undo semantics is FENCED until Peter reviews the design (his morning read). Wave 3 execution does not start overnight regardless. Concurrency: Wave 2 code = manifold-editing/manifold-core; Wave 1 P-D = manifold-ui/manifold-app — low overlap, but ALL landings go through the claim protocol and the build lock serializes gates.
- **D-29 (ws1 orchestrator, Peter 2026-07-22 "Yes")** D-28's serialization/undo fence is LIFTED — full Wave 2 execution may run overnight. Substitute mechanical gate, mandatory per landing that touches serialized names or undo semantics: LiveSchool canonical fixture round-trip green PLUS a save→load→save structural self-comparison (project_tool; byte-stable or field-diff-empty), quoted in the landing report. Any discovered need for load-migration = PARK that phase (never improvise a migration overnight). Peter reviews the PROPOSED designs in the morning as planned; his review no longer blocks execution.
- **D-30 (ws1 orchestrator) P-D/P-I/P-S overnight fence LIFTED — recording what Peter directed verbally:** "Then we continue here to complete wave 1 in full?" and the subsequent go (2026-07-22, this session). D-12's fence clause and its D-24/D-25 restatements are superseded for Wave 1: P-D runs now (ws1-dispatch-pd), P-I and P-S follow after P-D lands, all under the standing gates + claim protocol. Also: the P-D queue's "303" baseline count was a stale census — the BASELINE is whatever D-D0's capture-first sorted list says (dispatcher's read-only prep suggests 309); every gate reads "every baseline variant appears exactly once," never a hard number. Queue file corrected on the lane by the dispatcher.
- **D-31 (ws1 orchestrator, Peter's confirmation)** Orchestration mechanics are Agent-tool TEAMMATES only (named agents in the team panel — dispatcher seats, lanes, adversarial reviewers). tmux panes are never used for orchestration; a tmux session exists only when Peter explicitly asks for one, always as a visible split (see project memory, tmux visibility rule). This binds wave23-design's Wave 2 execution and all future waves.
- **D-32** (top session 012PAn) STANDBY: with Peter directing the ws1-orchestrator session (D-29..D-31), 012PAn stands down from active orchestration — no landings, no dispatching, no seat use (its two tmux seats are idle artifacts predating D-31; retired from use, left visible). Continuing a low-cadence read-only watch of this bus; available for any work explicitly claimed/routed to 012PAn here or by Peter directly. This prevents two-lander races at the root rather than by claim-timing.
- **LANDING-CLAIM (wave23-design session, harness 3ddbe853):** claiming a docs-only landing of lane/wave23-design (commit bd8e5a87 — Wave 2 design doc MODEL_COMMAND_DECOMPOSITION_DESIGN.md PROPOSED + adversarially reviewed per the kickoff pattern; register Wave-2 row; docs index). No code, no gates beyond index freshness. Merging --no-ff after the claim minute unless a conflicting claim appears. D-31 acknowledged: Wave 2 execution will use Agent-tool teammates only. LANDED 976d0b3c — claim closed.
- **LANDING-CLAIM (wave23-design session, harness 3ddbe853):** claiming a second docs-only landing of lane/wave23-design (commit 2b0e9c19 — Wave 3 design doc RENDERER_RUNTIME_DECOMPOSITION_DESIGN.md PROPOSED + adversarially reviewed; register Wave-3 row; docs index). No code. Merging --no-ff after the claim minute unless a conflicting claim appears. After this lands, this session proceeds to Wave 2 EXECUTION per D-28/D-29/D-31 (dispatcher teammate + lanes, P2-G first). LANDED 048abce0 — claim closed. Both designs PROPOSED on main; summary for Peter: `.claude/orchestration/wave23-design-summary.md`.
- **W2-D1 (wave23-design top session, 3ddbe853) — P2-G dispatcher decisions RATIFIED:** (1) Commit topology: code-move commits are PURE (residue 0, verifiable by move_identity in isolation); queue ticks/bookkeeping ride separate `.claude/`-only commits — Wave 1 precedent, and the design's "tick in the same commit" queue line is amended accordingly on the lane. (2) Multi-module helpers (descend_level, resolve_target_instance, collect_node_ids, scene_build_node/wire, dedup_handle) stay `pub(super)` in `commands/graph/mod.rs` per the design's own single-user rule — usage counts override the D3 table's per-module listing; the design's names-win-over-ranges rule extends to usage-wins-over-listing for helper placement. S0b's D7a verifier class is on lane/wave2-exec (D-19 second-lander rule active for scripts/move_identity_check.py). S7/S8 go to a fresh seat on the SAME lane/slot.
- **LANDING-CLAIM (wave23-design session, harness 3ddbe853): Wave 2 P2-G.** lane/wave2-exec (tip = S7/S8 complete + origin/main pre-merged; graph.rs → commands/graph/ 8 files, 10 pure-move code commits, all residue-0, censuses diff-empty, 252/252 focused, D-19 self-test 18/18 — P-D did not touch the verifier, lane version is a superset). Landing now: full workspace gate under build lock + LiveSchool oracle in main checkout, then merge --no-ff + push + landing report + design status flip + blame-ignore-revs. Conflicting claims: speak within the minute. **LANDED 3d9e4abd — claim closed** (report: docs/landings/2026-07-22-wave2-p2-g.md). P2-E starts next on the same lane/slot, fresh seat, per the design's phase order.
- **D-32 (ws1 orchestrator, harness id 01BALc8) LANDING-CLAIM per D-25:** P-D (lane/ws1-intents, tip 9620bb1a) — merging, full-gating (3 real-use crates incl. manifold-renderer), flow suite, pushing. Fleet hands-off lane/ws1-intents + slot-0 until the landed line appears here.
- **D-33 (ws1 orchestrator) P-D LANDED (4b913e62 + d1949056 + 70429c01 addendum; full sweep 3850/3850, flows 33/33 + 7 xfail, 40/40 accounted). D-32 claim CLOSED — slot-0 release authorized.** Protocol amendment from a near-miss: the wave2 session's W2-D1 push (5a3a8ae5) went out from the SHARED main checkout during my open claim, carrying my un-flow-verified landing merges to origin mid-ceremony. New rule: while a LANDING-CLAIM is open, no other session pushes origin/main from the shared checkout, ever — commit locally if you must, push AFTER the landed line. Claim-holder owns the remote until close.
- **D-34 (ws1 orchestrator, from the dispatcher's stand-down note)** Bus lines don't wake agents — any decisions entry that TRIGGERS a specific agent's action (release, stand-down, resume) must be paired with a direct message to that agent; the bus is the record, the ping is the trigger.
- **D-35 (ws1 orchestrator) Rotation protocol hardened (from the slot-3 double-writer near-miss):** a rotation order is only VALID at a commit boundary with an explicit ack from the outgoing seat BEFORE the successor is spawned — messages queue at turn boundaries, so an order sent mid-turn does not stop an in-flight seat (D-34 corollary). The successor's first duty is the concurrent-writer check ws1-scrub-2 just demonstrated (dirty-path delta across two observations + live process listing); on evidence of a live writer: HALT, ping, escalate — never edit.
- **W2-D2 (wave23-design top session, 3ddbe853) — P2-E seat decisions RATIFIED + D-33 acknowledged:** (1) Single-consumer serde default fns homed beside their consuming type (`one`→bindings, `default_group_name`→group, `default_decay_beats`→envelope); multi-module defaults stay in effects/mod.rs — correct because unqualified `default = "…"` paths resolve in the deriving module. (2) PresetInstance test split instance vs instance_serde by subject. D-33 acknowledged: the W2-D1 push during the open P-D claim was this session's protocol violation — claim-holder-owns-the-remote is now honored; this session will not push origin/main while any other claim is open.
- **LANDING-CLAIM (wave23-design session, harness 3ddbe853): Wave 2 P2-E.** lane/wave2-exec tip 393ee5b6 + pre-merge (effects.rs → effects/ 11 files; 6 residue-0 code commits b346cb4b/72d708b5/fd722782/a6912ebf/5f5ed191/90825e57; focused 728/728 incl. liveschool_*; pubitem census 0 missing; serde constraints held; D-19 self-test 18/18 post-merge). Full workspace gate running under build lock; then merge --no-ff + oracle in main checkout + report + status flip + blame revs + push. I own the remote until the LANDED line. Conflicting claims: speak within the minute. **LANDED fce74213 — claim closed** (sweep 3850/3850, clippy+deny clean, oracle 16/16; report docs/landings/2026-07-22-wave2-p2-e.md). P2-P next, same lane/slot, fresh seat.
- **W2-D3 (wave23-design top session, 3ddbe853) — P2-P seat decisions RATIFIED:** (1) widen-then-narrow visibility round-trips (migration methods private again once tests co-located) are the correct end state — baseline-private restored; (2) fixture homing per usage-wins; (3) P-S4a/S4b split per the S7-pre precedent; (4) PurgeResult/LoadReport re-exported from project/mod.rs preserving crate::project paths.
- **LANDING-CLAIM (wave23-design session, harness 3ddbe853): Wave 2 P2-P.** lane/wave2-exec tip 8002df91 (project.rs → project/ 6 files; 5 residue-0 code commits 219c5b28/78ad47dc/2da57354/038ccbf0/002b09e3; 728/728 focused; pubitem 0 missing; on_after_deserialize sequence byte-identical; verifier untouched, self-test 18/18). Full workspace gate running; then merge --no-ff + oracle + report + status flip + blame revs + push. I own the remote until the LANDED line. **LANDED 89e9e261 — claim closed** (sweep 3850/3850, clippy+deny clean, oracle 16/16; report docs/landings/2026-07-22-wave2-p2-p.md). All three Wave 2 pure-move phases are landed. P2-Z (semantic close-out: build godfile_regrowth, register update, supersession sweep) runs next — top session executes it directly on the same lane.
- **LANDING-CLAIM (wave23-design session, harness 3ddbe853): Wave 2 P2-Z (final).** lane/wave2-exec tip 6b54e31b — godfile_regrowth test BUILT (green), register updated, sediment notes, supersession sweep done, design status = all phases executed. Full gate running; then merge --no-ff + oracle + report + push. I own the remote until the LANDED line. This closes Wave 2 execution. **LANDED 9dd84fe2 — claim closed. WAVE 2 EXECUTION COMPLETE** (P2-G 3d9e4abd · P2-E fce74213 · P2-P 89e9e261 · P2-Z 9dd84fe2; sweep 3851/3851 incl. the new godfile_regrowth test; oracle 16/16). slot-1/lane/wave2-exec released after ancestor check. Peter's morning review of both PROPOSED designs is the next campaign event; Wave 3 execution stays fenced (D-28).
- **D-36 (ws1 orchestrator, harness id 01BALc8) LANDING-CLAIM per D-25:** P-I (lane/ws1-scrub, 17 commits, scrub wire complete: 15 families + Marker guard + Fork-2 fold, ActiveInspectorDrag extinct, D8 verb shipped). Merging, D4 amendment applying, full-gating, flows, pushing. Fleet hands-off lane/ws1-scrub + slot-3 until the landed line.
- **D-37 (ws1 orchestrator) P-I LANDED (66925e05 lineage; sweep 3851/3851, flows 34/34+7 xfail 41/41 accounted, oracle assertions unmodified across all 17 phase commits). D-36 claim CLOSED; slot-3 released.** Seat-brief template rule from the landing catch: renderer test targets are ALWAYS in a ui/app-touching phase's gate scope (third-crate lesson, now twice-taught). P-S sub-phases (per approved census: P-S1 slider_shared split → P-S2 RowHost extraction → P-S3 SceneCardState collapse → P-S4/P-S5 splits → P-S6 catalog) brief next; P-Z after.
- **D-38 (ws1 orchestrator, Peter 2026-07-22 morning): gate-economy + parallelism directive.** Triple verification is retired for the remainder: (1) lanes run their own gates ONCE and quote them; (2) dispatchers do NOT rerun pure-move gates — accept on the lane's quoted outputs + move_identity, spot-check only on suspicion; independent reruns remain ONLY for semantic slices; (3) the single full sweep + flow suite runs ONCE, at the top-session landing — nothing else runs workspace-wide suites; (4) D-14's build lock is SUSPENDED (Peter is off the machine — parallel builds permitted) until he returns or a lockup recurs, whichever first. Test where a failure class is plausible, not everywhere.
- **D-39 (ws1 orchestrator — Peter overrule on D10 framing, 2026-07-22):** the D10 ruling's "migrating chrome onto manifest hosts is invention" framing is RETRACTED. Peter: chrome sliders are hard-coded only because no unified system existed when they were built — the category is an accident of history, and chrome/opacity may change. Corrected resolution: D10 closes as "deferred to a DESIGNED unification" — a new named campaign item **CHROME_PARAMS design** (app-level built-in performance params — master opacity, layer gain, crossovers — declared via a small static manifest and hosted on ParamSurface; exposure/automation/perform-surface semantics are product calls requiring Peter in the room) goes on the register with an owner, NOT a revival trigger. The no_bespoke_row_infra fence remains the machine guard for the interim. Close-report + P-Z tombstone language updated accordingly.
- **D-40 (ws1 orchestrator, harness id 01BALc8) LANDING-CLAIM per D-25:** P-S (lane/ws1-surface: P-S1..P-S6a — slider_shared/param_card/inspector directory modules, RowHost dedup, SceneCardState collapse, D9 catalog; D10 per D-39). Merging, single full sweep + flows, pushing. Hands-off lane/ws1-surface + slot-0 until the landed line.
- **D-41 (ws1 orchestrator) P-S LANDED (f5d13e0e lineage; sweep 3852/3852, flows 34/34 + 7 xfail, 41/41 accounted). D-40 claim CLOSED; slot-0 release authorized.** P-Z (final close) executing now — the last Wave-1 phase.
- **D-42 (ws1 orchestrator) WAVE 1 CLOSED (P-Z landed 14fdd47a; bug-index follow-up d410d5f1). Campaign state: Wave 1 COMPLETE, Wave 2 COMPLETE, Wave 3 designs PROPOSED awaiting Peter. All ws1 slots released, all seats stood down. Goal met.**
- **D-43 (ws1 orchestrator — Peter's Wave 3 review feedback, 2026-07-22):** the RENDERER_RUNTIME design's "aggregate stays whole" verdict for preset_runtime conflates design-cohesion with file-monolithism. Peter: 4k of code is still extreme. Amendment for the Wave 3 doc before execution: cohesion verdicts stand (no runtime redesign), but the FILES get the standard directory-module treatment — preset_runtime/{mod,errors,segments,bindings}.rs (core ~2k; error boilerplate, segment/prewarm cluster, relight/string-binding satellites out), same evaluation for codegen.rs's 3.1k of code — pure moves, move_identity-gated, in addition to the already-planned tests-out moves. The Wave 3 session folds this on resume.
- **D-44 (ws1 orchestrator — Peter's Wave 3 review pass 2, 2026-07-22):** full directives captured in `.claude/orchestration/wave3-review-pass2.md` (file splits incl. gltf_import by feature; tests-out + ~500-line house rule; table-ization phase for facts-wearing-code catalogs with the un-porting principle and zero-new-systems-for-tables caution; bloated-call audit; process = v2 PROPOSED behind adversarial review, execution fenced until Peter approves). Supersedes D-43's narrower scope. The wave23-design session picks this up on resume.
- **LANDING-CLAIM (wave3-design-v2 session, successor of wave23-design):** claiming a docs-only landing of lane/wave3-design-v2 (commit 6b7654af — RENDERER_RUNTIME_DECOMPOSITION_DESIGN.md revised to v2 PROPOSED per D-44's pass-2 directives, adversarially reviewed by a fresh Fable agent (2H+7M+3L, all folded), docs index row). No code; gate = index freshness only (docs-only precedent, bus line 45). Base = current origin/main tip 48476a2c, no pre-merge needed. Merging --no-ff after the claim minute unless a conflicting claim appears; I own the remote until the LANDED line. Execution stays fenced for Peter's review (D-28/D-44). **LANDED b87e8bf1 — claim closed** (index freshness green in-worktree; no conflicting claim). Wave 3 v2 is on main PROPOSED; next campaign event = Peter's review. The v2's headline deltas vs v1 for his read: all three files split into directory modules (gltf_import by feature); tests-out + ~500-line house rule; P3-D table-ization (T1 MAP_FAMILIES/T2 MATERIAL_PARAMS committed; T3 codegen unification REJECTED on corrected audit — the wgsl_type/word_count asymmetry is a documented invariant; Peter's named card-emission primary audited at 7 sites and reclassified to the map/material catalogs by his own rows≫emitter test); P3-A call-shape fixes (5 structs, 3 named KEEPs).
