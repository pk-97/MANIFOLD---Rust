# Wave 2 execution queue — P2-G (commands/graph.rs → commands/graph/)

Design (BINDING, read §2 D1/D3/D7/D7a + §4 P2-G + forbidden lists before any edit): `docs/MODEL_COMMAND_DECOMPOSITION_DESIGN.md`. Orchestration rules: decisions bus D-13 (command hygiene), D-14 (build lock for heavy runs), D-17 (this queue lives on the lane, checkbox commits ride the lane), D-19 (move_identity_check is a shared gate — second lander merges both pattern sets), D-29 (park on any load-migration need), D-31 (teammates only). Lane: `lane/wave2-exec`, slot-ring worktree. ONE commit per slice, then stop for review. Lanes never land — the top session (wave23-design, harness 3ddbe853) merges.

Per-slice gate (exit codes, no eyeballs): `python3 scripts/move_identity_check.py <commit>` → residue 0; multiset command census unchanged (see S0a); `cargo nextest run -p manifold-editing` green (zero test-body diffs); at phase end `cargo clippy -p manifold-editing -- -D warnings`.

- [x] **S0a — Baselines (capture-first, no code change).** In the worktree: `rg -o 'impl Command for [A-Za-z_0-9]+' crates/manifold-editing/src/commands/ --no-filename | sort | uniq -c > .claude/orchestration/wave2-census-baseline.txt` and the public-item census of graph.rs per design D7 (widened regex) into `wave2-pubitem-baseline.txt`. Commit both on the lane. Expected: 161 raw / 29 in graph.rs — if not, STOP, re-list, record in queue.
- [x] **S0b — D7a verifier extension (tooling commit; scripts/ shared gate, D-19).** Add to `scripts/move_identity_check.py` a narrow test-mod wiring ALLOW class: `#[cfg(test)]` / `#[cfg(all(test, feature = "…"))]` attribute lines and bare `mod <name> {` / closing `}` opener-closer pairs ATTACHED TO TEST MODS only; any non-mod line under the class = residue. Two new self-test fixtures: (1) test distribution → PROVEN, (2) code line smuggled into a test-mod header → CAUGHT. Run the FULL self-test suite. If Wave 1 lanes have edited the script since baseline, merge BOTH pattern sets first (D-19).
- [x] **S1 — `git mv` graph.rs → graph/mod.rs.** Pure rename, zero content change. Gate: build green, censuses unchanged.
- [x] **S2 — `node_edit.rs`** out of mod.rs: the 9 node-edit commands + `subtree_node_ids` + `exposed_param_labels_for_node` (design D3 row 2; mod.rs keeps `pub use` for the pub fn — app_render.rs:2168 calls it).
- [x] **S3 — `expose.rs`**: ToggleNodeParamExpose + reverse enums + mirror/unmirror + `find_node_by_id_or_handle_mut`/`effective_value_to_serialized` (design D3 row 3; both helpers are expose-region-only, review-verified).
- [x] **S4 — `groups.rs`**: GroupNodes/UngroupNode/SetGroupTint/RenameGroup + group helpers (design D3 row 4). CENSUS-CRITICAL slice: the multiset census exists because a second `RenameGroupCommand` lives in commands/effect_groups.rs — a set-census is blind here. Verify raw count 161 preserved.
- [x] **S5 — `scene.rs`**: the 11 scene commands + scene builders/id helpers (design D3 row 5).
- [x] **S6 — `modifiers.rs` + `paste.rs`** (design D3 rows 6–7).
- [ ] **S7 — `test_support.rs` + test distribution.** Requires S0b landed on this lane. Shared fixtures → `test_support.rs` (`#[cfg(test)]`), per-module `mod tests` beside their commands, bodies verbatim, cfg on declarations. The ONLY sanctioned edits are the S0b wiring classes. A fixture may NOT be copied — import from test_support.
- [ ] **S8 — Phase-end lane gate.** Full `cargo nextest run -p manifold-editing` + clippy `-p manifold-editing -- -D warnings` + both multiset censuses vs baselines + deletion proof (`test -f crates/manifold-editing/src/commands/graph.rs` exits 1, `test -d .../graph` exits 0). Report all outputs in the final message. STOP — top session lands.

## S7/S8 HANDOFF (this dispatcher stopped after S6 — fresh seat picks up S7)
S0a–S6 are DONE and green (14 lane commits on lane/wave2-exec; every code commit move_identity residue 0, MULTISET command census diff-empty vs baseline, pubitem 0-baseline-missing, nextest 252, clippy lib+tests clean, self-test 18/18). All 6 command modules extracted; graph.rs is gone; graph/ = {mod,node_edit,expose,groups,scene,modifiers,paste}.rs. mod.rs is now 5072 lines = ~294 shared helpers + re-exports + one flat `#[cfg(test)] mod tests` (~2778 lines, starts line 294). Stopped here per window-economy/fresh-session discipline — S7 is a large verbatim-body distribution best done with fresh context.

**S7 groundwork (for the next seat):**
- Shared FIXTURES to move to `test_support.rs` (`#[cfg(test)]`, `pub(super)`, import via `use super::super::test_support::*` — NEVER copy a fixture, D3 forbidden): `slot`, `abc_graph`, `project_with_graph`, `graph_of`, `mirror_catalog_default`, `project_with_one_master_effect`, `project_with_one_generator_layer` (re-scan the mod for any others before the header of the last test block).
- Test→module assignment by name-prefix (verify each against the command it constructs): `group_nodes_*`/`ungroup_*`/`scoped_move_*` → groups; `add_graph_node_*`/`remove_graph_node_*`/`connect_ports_*`/`disconnect_ports_*`/`move_graph_node_*`/`layout_graph_nodes_*`/`scoped_add_node_*`/`set_graph_node_param_*`/`set_wgsl_source_*`/`revert_*`/`graph_edits_survive_json_round_trip` → node_edit; `toggle_node_param_expose_*`/`exposing_*`/`unexposing_*`/`unchecking_*`/`unexpose_*` → expose; scene-object/light/env/fog/import/rename tests → scene; mesh-modifier tests → modifiers; paste tests → paste. The generator-target tests (`*_against_generator_*`) go with the command they exercise (add/revert → node_edit; toggle/expose → expose).
- Use S0b's D7a test-mod wiring class (already landed, self-test 18/18). Per-module test mod = `#[cfg(test)] mod tests { use super::*; use super::super::test_support::*; <tests verbatim> }`. Expect per-module import iteration exactly like S5/S6 (the test-only `use crate::command::Command;` + `effect_graph_def::{GROUP_TYPE_ID, GroupDef, ...}` that currently sit inside mod.rs's flat test mod at ~lines 300-308 must be re-homed into whichever module test mods actually use them; clippy --tests is the oracle).
- Gate per D7a-aware move_identity (residue 0), MULTISET command census unchanged (test moves do not touch `impl Command for`), nextest 252, clippy lib+tests. Commit topology: one CODE commit (residue 0), separate queue tick.
- S8 after S7: full nextest + clippy --tests + `cargo deny check bans` (if landing) + both censuses + deletion proof (graph.rs test -f exits 1 = already true; graph/ test -d exits 0 = true). Then STOP — top session lands.

## Dispatcher process notes (rides the lane; for restart-me + the top session)
- **Commit topology (resolves a brief vs gate conflict):** the brief says "tick the queue checkbox in the same commit," but riding the `- [ ]`→`- [x]` line in a code-move commit makes `move_identity_check <commit>` permanently red (the checkbox line is non-wiring residue), defeating INV-M1's exit-code gate. Wave 1 slice commits (e.g. 1f5c9247) carried CODE ONLY. So here: each code-move slice is a PURE code commit (residue 0), and the queue tick + these notes ride a SEPARATE `.claude/`-only bookkeeping commit right after (still on the lane, restart-proof per D-17). Top session: the "same commit" wording in the brief is superseded by this — verify each CODE commit is a clean move.
- **Pubitem census reading (D7/INV-M5):** the split legitimately ADDS wiring the flat baseline lacked — `pub use <mod>::*` facades and `pub(super)` helper bumps — so the raw pubitem COUNT grows every slice; it is NOT a fixed-count equality gate. The gate is: every BASELINE public item still appears (comm -23 baseline vs new = empty), and every ADDITION is sanctioned wiring (`pub use` / `pub(super)`), never a real new/removed public command. S2 delta was exactly `pub use node_edit` + 5 `pub(super)` helpers, zero baseline items missing.
- **S4 helper placement (DECISION for top-session ratification):** D3's groups.rs row lists `descend_level`, `innermost_group_display_name`, `collect_node_ids`, `resolve_target_instance` as moving into groups.rs. Verified usage contradicts that: all four are used across node_edit/expose/scene as well as groups (descend_level 41×, resolve_target_instance 8×, collect_node_ids 6× in mod.rs). The design's OWN rule "cross-module helper visibility is pub(super); helpers used by exactly one module move into it" (contrapositive: multi-module helpers stay pub(super)) resolves the table-vs-usage conflict → I kept all four in `graph/mod.rs` as `pub(super)` shared helpers; groups.rs (and node_edit/expose) reach them via `super`. No node_edit/expose import churn was needed. If the top session prefers the literal table (helpers → groups.rs), that's a trivial follow-up (move 4 fns + rewire ~4 use lines); flagging for ratification, not blocking.

Forbidden (design §4, verbatim): reshaping any command body · widening a helper beyond `pub(super)` without a bus ruling · copying fixtures · scene-command dedup · any semantic change sharing a commit with a move · touching serde attributes · landing (lanes never land). Anything the design + bus don't cover → PARK in `.claude/orchestration/parked.md` on the lane and continue with the next slice.
