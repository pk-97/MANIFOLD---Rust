# WS1 P-D queue — intent-enum decomposition (overnight 2026-07-22)

Design authority: `docs/UI_FUNNEL_DECOMPOSITION_DESIGN.md` D5 (sum enum, one wire type, `From` impls, variant bodies VERBATIM — re-parenting only; any body reshaping is P-I's job and strictly forbidden here). Dispatcher: pop top-to-bottom, one Sonnet lane per slice (D-D1 is Opus-grade — see entry), slot ring worktree on `lane/ws1-intents`, pathspec commits, decisions bus rules (D-1..D-30, read fresh from main) apply.

GATES per slice (exit codes, quoted in commit messages): compiler is the checklist (exhaustive matches); variant-census: every baseline `PanelAction` variant NAME (baseline = D-D0's capture-first sorted list, no hard number — D-30) appears exactly once across the domain enums (sorted-LIST diff vs baseline, not just count — script the diff); `cargo clippy -p manifold-ui -p manifold-app --tests -- -D warnings`; `cargo nextest run -p manifold-app -p manifold-ui`; oracle suites (`undo_baseline`, `mapping_undo_baseline`, `bug_266_tab_pin`, `dispatch_chain_completeness`) green with assertions UNMODIFIED. move_identity does NOT gate P-D (re-parenting is the sanctioned semantic phase); the census LIST diff is the completeness oracle.

- [x] **D-D0 derivation (lane, mechanical, no code):** for each enum variant (baseline: `panels/mod.rs` `pub enum PanelAction`, capture the sorted list FIRST — that list IS the baseline, no hard count per D-30; dispatcher's read-only prep suggests ~309), derive its domain = the handler file whose match holds its ARM (`rg 'PanelAction::(\w+)' -o` on arm-heads per handler file: dispatch/{browser,clip,params,modulation,mapping,audio_setup}.rs + ui_bridge/{editing,layer,marker,project,transport}.rs + mod.rs's own arms). Variants with arms in mod.rs or nowhere → `Root` bucket (list them explicitly — expect SliderReset + UI-internal ones). Commit the mapping as `.claude/orchestration/pd-partition.md` on the lane. STOP for dispatcher check: bucket sums must total the captured baseline list-length exactly, every baseline variant in exactly one bucket, no variant in two buckets (sorted-LIST diff, not just count).
- [x] **D-D1 enums + wire (Opus-grade slice — the one judgment commit):** DONE — flat 12 enums in `panels/actions.rs`, PanelAction thin sum, 12 From impls, router = 12-arm exhaustive match (Root inline), 11 handlers take `&<Domain>Action`, ~783 sites wrapped, dispatch_inspector chain + chain_completeness test RETIRED (team-lead flat-12 ruling; supersedes the "keep until P-Z" note below). Commits 9d9471db/c233207c/f5ef1a29. Dispatcher independently reran gates: census 303/303 disjoint, clippy -p ui -p app --tests -D warnings clean, nextest 1172/0/3, oracle trio green & files untouched, chain absent, manifold-renderer test target compiles (3rd crate). ORIGINAL TASK: define per-domain enums in `panels/mod.rs` (or `panels/actions/` submodules — executor's call, record it): `BrowserAction, ClipAction, ParamsAction, ModulationAction, MappingAction, AudioSetupAction, EditingAction, LayerAction, MarkerAction, ProjectAction, TransportAction, RootAction` — variant DEFINITIONS moved verbatim (fields, docs, serde-irrelevant: these never serialize — verify with a `Serialize|Deserialize` rg on the enum first, STOP if any hit). `PanelAction` becomes the sum: one variant per domain + `From<DomainAction>` impls. Compiler-driven: land the enum change, then fix every red site mechanically — emit sites become `PanelAction::Params(ParamsAction::X)` (or `.into()` where a builder returns the domain type naturally; pick ONE style per file, prefer explicit). Match sites: each dispatch handler now matches its OWN domain enum after destructuring at the router — router destructures the sum ONCE and passes the domain value; chain becomes a match on the sum (exhaustive — the compiler now proves routing, superseding chain-completeness for domains; keep the invariant test until P-Z). This slice may need several sessions of one Opus seat — commit at every compiling checkpoint.
- [x] **D-D2 emit-site sweep completion:** STRUCK — D-D1 landed fully green (f5ef1a29), nothing parked, no deviations recorded, emit-site style consistent (explicit wrapping). Nothing to normalize.
- [x] **D-D3 scrub-trio annotation (NO code):** DONE (in-context, computable per team-lead endorsement) — appended to `pd-partition.md`: 15 full Snapshot/Changed/Commit trios + 3 infix-split reconstructed trios (18 real) + 12 genuine non-trio partials, each with domain. P-I's kill list.
- [ ] **D-D9 phase close:** census LIST diff empty vs baseline; full-lane gates; consolidated report to team-lead for landing (landing-claim protocol applies).

Escalation: anything that resists verbatim re-parenting (a variant whose fields must change, a match that can't destructure cleanly) → PARK, continue other slices only if independent, else report. No reshaping, ever — that is P-I.
