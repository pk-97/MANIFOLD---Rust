# WS1 P-D queue — intent-enum decomposition (overnight 2026-07-22)

Design authority: `docs/UI_FUNNEL_DECOMPOSITION_DESIGN.md` D5 (sum enum, one wire type, `From` impls, variant bodies VERBATIM — re-parenting only; any body reshaping is P-I's job and strictly forbidden here). Dispatcher: pop top-to-bottom, one Sonnet lane per slice (D-D1 is Opus-grade — see entry), slot ring worktree on `lane/ws1-intents`, pathspec commits, decisions bus rules D-1..D-27 apply (read fresh from main).

GATES per slice (exit codes, quoted in commit messages): compiler is the checklist (exhaustive matches); variant-census: every one of the 303 baseline `PanelAction` variant NAMES appears exactly once across the domain enums (sorted-LIST diff vs baseline, not just count — script the diff); `cargo clippy -p manifold-ui -p manifold-app --tests -- -D warnings`; `cargo nextest run -p manifold-app -p manifold-ui`; oracle suites (`undo_baseline`, `mapping_undo_baseline`, `bug_266_tab_pin`, `dispatch_chain_completeness`) green with assertions UNMODIFIED. move_identity does NOT gate P-D (re-parenting is the sanctioned semantic phase); the census LIST diff is the completeness oracle.

- [ ] **D-D0 derivation (lane, mechanical, no code):** for each of the 303 enum variants (baseline: `panels/mod.rs` `pub enum PanelAction`, capture the sorted list FIRST), derive its domain = the handler file whose match holds its ARM (`rg 'PanelAction::(\w+)' -o` on arm-heads per handler file: dispatch/{browser,clip,params,modulation,mapping,audio_setup}.rs + ui_bridge/{editing,layer,marker,project,transport}.rs + mod.rs's own arms). Variants with arms in mod.rs or nowhere → `Root` bucket (list them explicitly — expect SliderReset + UI-internal ones). Commit the mapping as `.claude/orchestration/pd-partition.md` on the lane. STOP for dispatcher check: bucket sums must total 303 exactly, no variant in two buckets.
- [ ] **D-D1 enums + wire (Opus-grade slice — the one judgment commit):** define per-domain enums in `panels/mod.rs` (or `panels/actions/` submodules — executor's call, record it): `BrowserAction, ClipAction, ParamsAction, ModulationAction, MappingAction, AudioSetupAction, EditingAction, LayerAction, MarkerAction, ProjectAction, TransportAction, RootAction` — variant DEFINITIONS moved verbatim (fields, docs, serde-irrelevant: these never serialize — verify with a `Serialize|Deserialize` rg on the enum first, STOP if any hit). `PanelAction` becomes the sum: one variant per domain + `From<DomainAction>` impls. Compiler-driven: land the enum change, then fix every red site mechanically — emit sites become `PanelAction::Params(ParamsAction::X)` (or `.into()` where a builder returns the domain type naturally; pick ONE style per file, prefer explicit). Match sites: each dispatch handler now matches its OWN domain enum after destructuring at the router — router destructures the sum ONCE and passes the domain value; chain becomes a match on the sum (exhaustive — the compiler now proves routing, superseding chain-completeness for domains; keep the invariant test until P-Z). This slice may need several sessions of one Opus seat — commit at every compiling checkpoint.
- [ ] **D-D2 emit-site sweep completion:** whatever D-D1 left red or ugly-but-compiling (allowed interim: none — D-D1 must land green; D-D2 is style normalization ONLY if D-D1 recorded deviations, else strike).
- [ ] **D-D3 scrub-trio annotation (NO code):** annotate in `pd-partition.md` which variants are Snapshot/Changed/Commit trio members (≈30 trios) — P-I's kill list, pre-derived while the knowledge is warm.
- [ ] **D-D9 phase close:** census LIST diff empty vs baseline; full-lane gates; consolidated report to team-lead for landing (landing-claim protocol applies).

Escalation: anything that resists verbatim re-parenting (a variant whose fields must change, a match that can't destructure cleanly) → PARK, continue other slices only if independent, else report. No reshaping, ever — that is P-I.
