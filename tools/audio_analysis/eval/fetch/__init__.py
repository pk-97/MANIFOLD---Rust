"""One script per dataset: download -> verify checksums -> extract only the
manifest-listed tracks -> distill -> delete bulk (§3). Never redistributes
dataset audio (D8) — fetch scripts + checksums are committed, audio never is.
"""
