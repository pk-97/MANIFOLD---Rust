# Overnight masks and gestures — September 12, 8a5d674e2

Committed locally on codex/unified-scene-modifiers in slot-9; release build passed. Main unchanged, not pushed or landed. New MaskedPeel, OrderedReconHit, WavesEchoes recipes; five stock mesh recipes gain band/sphere masks. MorphMesh has opt-in frame interpolation. EnvelopeBeats exposes live attack/hold/release/tail and elapsed beats; new GPU stagger envelope consumes it. Symmetry, Merge, Contour Reveal, texture relief and RT are deferred. Saved modifier cards retain snapshots: re-add factory recipes for new controls.

Validation: renderer/app focused clippy passed before final test-only and JSON corrections. 15 focused primitive/beat tests passed. Native stagger-envelope, old/new MorphMesh and fused/standalone numeric proofs passed. GPU gate is NOT green: attempt1 needed missing PrimitiveSpec import; attempt2 mask numeric test incorrectly assumed yaw rotates Y, fixed by pitch90+yaw90 but unrerun. Recipe integration is NOT green: attempt1 missing metadata min/max; attempt2 mask binding targets prefixed mask_ rather than primitive names. Exact fixes applied to all eight recipes plus EnumRound, statically audited; no third attempt under budget. Recipe parse/default-mask check passed under GPU filter after fixes. Full graph preparation/runtime of all new recipes and corrected mask proof are the only new focused verification gaps, tracked BUG-e3p6.5. No new observed render/performance claim. No broad/system/native sweeps authorized. Release launch: ./target/release/manifold from slot-9.

All workers returned. Root stopped bounded check retries. Release build passed after production fixes. Remaining untracked handoff is intentional; do not stage. Prior status below is historical.

# Current creative slice — September 12, c1a2db88b

Surface Waves, Ordered Recon and Spatial Echoes are committed locally on codex/unified-scene-modifiers. Device-aware aggregate admission replaces the 256 MiB per-scene cap. Release build passed; launch from this slot with `./target/release/manifold`. Main is unchanged and branch is not pushed; prior guard requires app delivery through land_branch.py, whose full qualification remains deferred by Peter.

Validation: 21 focused app tests, 13 renderer tests, 4 real-photoscan import/roundtrip/preparation tests passed. The filtered structured_modifier GPU gate passed outside sandbox: four native numeric proofs, three explicit gather/fusion-boundary checks plus echo schema/unit tests. Sandbox initially reported no Metal device. Clippy stopped after two attempts; reported PI literals, echo test imports and prior replace_box lint were corrected, but no final clean clippy run claimed. Visual tuning and full-system/performance qualification remain with Peter and BUG-e3p6.5. RT remains separate in BUG-e3p6.4. Do not repeat passed checks or revive black-baseline investigations without new evidence.

Earlier work follows for reference.

# Prior card/preset cleanup — September 12

Peter narrowed this pass to shared card interactions, correct preset divergence/lifecycle, Surface Peel clip-hit catalog variation, and STATIC review instead of full system/native/performance tests. Positive user release render (hornet/Elastic Sculpture with LFOs) is established. Do not revive automated black-baseline investigations here.

Two Luna high lanes completed UI card reuse and core/editing group transactions. Root added shared preset dispatch/menu, scene-initialized catalog baseline comparison, deferred content-only modifier preset commands, keyboard delete/select-all integration, and corrected clip-hit envelope zero baseline. New files: ui_bridge/dispatch/presets.rs and stock SurfacePeelHit.json. Existing extensive F4-F8 dirt is all this workstream; main untouched. Do NOT stage this handoff.

Checks current: 34 focused core/editing/UI/app CPU tests PASS (/tmp/modifier-polish-cpu-retry.log). One missing app action arm was fixed after initial CPU compilation. Two clippy attempts stopped on UI type/privacy/lint errors; exact reported lines are corrected, but do not report final clippy green. Logs /tmp/modifier-polish-clippy.log and /tmp/modifier-polish-clippy-retry.log. Recipe CPU pair PASS: 6 tests (/tmp/modifier-polish-recipes.log), total 40 focused CPU checks green. Release build PASS (30.73s; /tmp/modifier-polish-release.log). Commit 05d1fa10e contains the integration. Only this handoff remains untracked. Branch push was blocked by PreToolUse: raw app pushes must use land_branch.py. Do not bypass; main remains unchanged. Final clean clippy and normal landing gate remain pending. No native/UI/render/performance runs authorised. Prior proof logs remain evidence for unchanged paths. Keep branch/worktree delivery; no full landing system gates in this pass.

# Active worktree handoff (untracked; do not land)

## Latest continuation (supersedes older status below)

- Main remains unchanged; F4–F8 edits are still uncommitted in slot-9. No landing gate or push yet. Two Luna lanes have returned; no workers editing.
- NEW PresetRuntime::prepared_modifier_buffer_usage uses the exact pure allocator plan and unchanged256MiB budget. App validate_owner checks it before candidate publication using project canvas dimensions. Oversized full-mushroom2Elastic now rejects atomically with requested494979072/allowed268435456.20admission/editing tests PASS `/tmp/unified-f8-budget-admission.log`. Duplicate instance/frame tests use supported SurfacePeel, not artificially raised caps.
- F7 latest two-stage conformance PASS `/tmp/unified-f7-unique-stage-ids.log`. First realScene-stage revision duplicated group_output stableID; fixed control output to hit_group_output and checked all recipe IDs unique. Runtime has no recipe-specific registration.
- Renderer+app clippy --tests with journey-proofs,perf-soak,ui-snapshot PASS `/tmp/unified-f8-feature-clippy-verified.log` before last trivial harness warmup/Play and mushroomscene pre-arm changes. UI build after pre-arm changes PASS `/tmp/unified-f8-ui-build-armed.log`.
- Native journey is ROOT-owned app/src/scene_modifier_journey.rs: two SurfacePeel, lift LFO, reorder/remove/undo/save/load/postloadmapping. The first frame still BLACK. Logs `/tmp/unified-f8-native-supported.log`, `/tmp/unified-f8-native-warmup.log`. Production run_warmup added beforeplay andafterreload; removes coldcompiles but does NOT fix black. No further native retries without a specific established cause. Native tests are featuregated, unqualified, not ignored.
- V8 app/src/scene_modifier_performance.rs (registered main.rs) built and ran with journey-proofs,perf-soak. Full mushroom NO MODIFIERS also BLACK. Stopped after baseline; no modifier or heldout measurements. Empty-output timings INVALID. Report actual location **crates/manifold-app/target/journey-proofs/f8_performance/photoscan-performance.json** (cargo test cwd is crate). AppleM4Max;644504triangles. Log `/tmp/unified-f8-performance.log`. Harness currently measures240frames before validating output; next fix should fail output gate BEFORE240frames, preserving failure report. Normal productionwarmup now called andPlay before8preparationframes.
- This narrows failure to baseline/harness/rendering seam, NOT established modifier regression. Static review: imported model/texture stringdefaults correctlyseeded; RT boolconversionscorrect; source guard warns ifviolated, noneobserved. GltfMeshSource::warmup_pending only checks pending_load, omits copy_in_flight/published, though it marks outputpending until nextframe. This could explain one initialframe but NOT240blackframes; do not claimrootcause or patchspeculatively. Manual fixture attaches graph to PhotoscanBaseline override, actualimport uses embeddedregisteredimportID; construction parity gap remains. Next boundeddiagnosis distinguish generator target from compositor output, or make fixture use exactImportModelLayerCommand beforeobserving. Preserve strictpositiveoracle.
- UI new mushroomscene shares imported_gltf_scene helper while originalgltfscene staysazalea. Mapped only scene-modifier-preset. Its supportedtwo-cardstack is Elastic+Fog. Firstnewfixtureattempt lackedprearmedinspector; addedmushroomscene in mod.rs+script.rs pre-armconditions. ThenflowpassedApply/Objects/Phase/Undo/AddFog/Move/Remove dispatch, failedCount because160msdelete-collapsecardretained. AddedStep12 (200ms) afterremove/undo/redo. FINAL UI verification PASS61/61steps, log `/tmp/unified-f8-ui-flow-settled.log`; all5presets plusApply/Objects/Phase/Undo/move/remove verified. Geometryplayback remainsseparate. No rerunneeded.
- Updated FOUNDATION_PLAN andVALIDATION_PLAN headers/evidence; keep F7playingUI/F8/native/audio/performance unqualified. Added actualmushroomcounts/capfinding withoutchangingceilings. BUG-e3p6.5 appendnotes tracks nativeblankbaseline andadmissionfix; remainsopen. Mainunrelatedbeadsedits preserved.
- GPU synthetic7pair migration parity and9atom proofs frompreviouscontinuation remainpassed; no rerunnecessary. Existing app/editing/UI tests remain as below.

User authorised unified scene modifier foundations F0–F8, two Luna high lanes, efficient checks/context. Stay in this task after compaction. Work NOT complete, NOT landed/pushed. Main unchanged. No goal/automation/new task.

Worktree slot-9, branch codex/unified-scene-modifiers, lease codex-unified-scene-modifiers, base 03c0a4d73b418de3139b58d04f9b2df0209e1ff4. HEAD 67ec65959; many coherent F4–F6 integration changes remain uncommitted. Prior summary carries commits through that tip. Preserve main unrelated beads edits. Exact-path commits only; guard needs explicit git -C slot9; land via land_branch.py after required gates.

## Current lanes
- modifier_report: completed F7 SurfacePeelHit.json + scene_modifier_file_authoring.rs. Lead ownership resumed. First test PASSED before latest two-stage extraction; latest file now Scene hit_control_stage → EachObject peel_stage with exact StageOutput broadcast, rerun required. No production runtime added.
- source_identity: running NEW app/src/scene_modifier_performance.rs only, brief /tmp/unified-f8-performance-brief.txt. No Cargo/GPU/commits. Implements V8 bounded240frame profiling, baseline then permodifier+stack, fail/stop if baseline overbudget. Root diagnoses native journey. Worker must receive construction fix before runs.

## Current process
Cargo/native diagnostic session11074 running (as of handoff write):
env RUSTC_WRAPPER= cargo test -p manifold-app --bin manifold --features journey-proofs f8_playing_photoscan_lfo_reorder_save_reopen_journey -- --test-threads=1 --nocapture
log /tmp/unified-f8-native-diagnostic.log. First native run failed uniform output at first4frame capture; now logger, activeclip/readiness/live-node assertions added to diagnose. Two runtime attempts max before static cause/necessary fix. No other processes.
All native Metal checks require require_escalated; sandbox returns No Metal device found. Host run works. Keep GPU/Cargo serialized. Cargo env RUSTC_WRAPPER=. Guard permits from MAIN absolute .codex/hooks/guard.py, not slot copy. Do not rerun passing checks unchanged.

## Verified this continuation
- /tmp/unified-f5-admission-fixed.log:17 prior app admission tests PASS;2 new clip tests initially fixturefailed only.
- /tmp/unified-f5-clip-tests.log:2 PASS, actual mushroom path comes from metadata string_bindings, not node.params. clip.layer_id set.
- /tmp/unified-f5-editing-guards.log:9 PASS Composite rollback/order and calibrated source/RT locks.
- /tmp/unified-f5-integrated-clippy-fixed.log: app/editing/UI clippy --tests PASS (later comments/featuretest added).
- /tmp/unified-f6-retired-tests.log: photoscan3 + importLoop1 + idempotence1 PASS. Roundtrip initially wrongly expected Float bars; fixed to exact Int8 plus reopened expanded graph/routes equality.
- /tmp/unified-f6-retired-remaining.log: roundtrip2, inv4, legacy3, photoscan migration9, stock5 PASS.
- Renderer clippy /tmp/unified-f6-retirement-clippy.log failed ONLY common helper collapsible-if; FIXED, rerun outstanding.
- /tmp/unified-f5-ui-build-fixed.log: app ui-snapshot build PASS after host_target Some() call fix in ui_snapshot/render.rs; removed preexisting unnecessary mut in fixtures.rs.
- /tmp/unified-f7-authoring-tests.log:1 PASS BEFORE final Scene stage extraction (latest now requires rerun).
- /tmp/unified-f6-gpu-parity.log:14 unit tests incl9existing photoscan GPU primitives PASS. Initial new parity test falsely passed uniform magenta; REJECT this as parity evidence.
- /tmp/unified-f6-gpu-parity-verified.log:5 migration-upgrade unit tests +1 new GPU test PASS. New GPU test runs7 frozen v2 pairs (3photoscan+stack+Loop+Fog+LoopFog). Fixtures synthetic lacked required PBR environment. Test adds same node.bake_environment to both BEFORE migration without touching fixtures, renders with actual PresetRuntime, asserts runtime.errors empty and nonuniform output then exact pixel equality. All7pairs pass; /tmp/unified-scene-modifier-parity/*.png. Lead viewed photoscan_stack-migrated.png: real visible distorted triangle geometry. Synthetic parity only, not realscan qualification.

## UI flow state
New scripts/ui-flows/scene-modifier-preset.json on gltfscene realazalea UI fixture (no playingclip) exercises all5, duplicate, move/remove/undo, Phase gesture, Objects menu. First host run failed expectedmenu lacked tick; corrected. Second /tmp/unified-f5-ui-flow-verified.log passed Apply/Objects/Phase/undo then duplicate picker matched card Label+menu Button. Corrected all preset Pointer queries with type Button and reviewed remaining steps; NOT rerun since correction. Two flow failures so no speculative retries; next necessary verification after concrete selector fix requires named bounded justification/permit ifguard asks. Existing scene-setup-modifier.json and scene-setup-modifier-trigger.json updated hardcoded old macro IDs to structural under_text selectors, unrun. Old meshstack Twist/Bend flow untouched. New UI flow file syntax/diff checks PASS. Output target/ui-snapshots/gltfscene/run-scene-modifier-preset. This is UI compositor, not playing geometry.

## Significant root fixes since prior summary
Shared object commands scene.rs: worker code compile errors corrected (NodeId HashSet, snapshot capture closure, helper import), critically prune_scene_object_metadata RETURNS PARAM IDs now passed to prune_instance_params (worker accidentally returned nodeIDs). Remove stores named RemovedObjectSnapshot, prunes public specs only when no surviving alias, restores instance+metadata onundo; explicit target removal sets rejection_reason, no stderr-only silent refusal. Duplicate caches after nodes/wires/stringbindings for stable redo and checks baseline before redoing.17app tests pass.
CompositeCommand: rollback prior appliedchildren and skip laterchildren on rejection; no constructor appliedbool (must preserve alreadyexecuted record callers),9tests pass.
Core+editing+UI source guards for direct node/macrowrites; clip string commands declare graph_admission_clips, adapter resolves owningGenerator and validates privately. Generic Live controls unchanged.19app tests pass.
New UI snapshot Runner drains content-owned SceneModifier by build_action+admission+execute, ordinary Execute still record-only. Additional UI force sync after content action. New Objects semanticbutton selection and preview exact IDs. Cards object enumeration now logs error before empty menu instead of silent unwrap_default.
Legacy core/editing/renderer factories deleted, instancesnapshot extracted, all5stock availabletrue in worktree. Negative rg old descriptors/builders/RecipeKind returnszero production references. Project load invokes coordinator once after object/exposure migration, refreshmanifest. Old tests ported to v3; common helper explained dead_code allowance because included subset pertest. Real photoscan migration assertions strengthened nonvacuous sourcenodes and completebindingpreservation.
Docs FOUNDATION_PLAN status/F3 updated to current evidence, programme implementationauthorisation updated, framework successor inprogress notlanded. Full reconciliation/beads/landing remains.

## New native journey (root-owned)
crates/manifold-app/src/scene_modifier_journey.rs registered in main.rs under all(test,journey-proofs,macos). One mushroom layer; heldout japanese_thistle IMPORT ONLY (render remains open).2Elastic, LFO, move/remove/undo, saver/loader then ContentCommand::LoadProject sameGPU, samebeat mapping change. Explicit clip.layer_id. Root changed raw shear_x lookup to exact prepared.routes modifier ID + allobjectcopies. PipelineCompile counter snapshots after4frameprep and asserts no extra over20liveframes. Captures RGB/alpha finite, rejects uniform and writes PNG before assertion. Does not yet test audio, real first clipHit, heldout render, budget240frames. Need finish these required proofs without claiming broad qualification.
Journey featureclippy /tmp/unified-f8-journey-clippy.log found unused corridor variables/helpers only. Removed stale bindings and collapsed helper if. native_gpu_for_tests existing unused method now used by journey capture. Corrected corridor param_f32 to manifold_core::effects::serialized_value_as_f32 (initial wrongmodule caused /tmp/unified-f8-journey-clippy-fixed.log; fixed, not yet clippy repeated). Actualnative test compiles.

## Immediate next
Read native diagnostic result, statically fix actual cause before another run. Send established construction fix to perfworker. Rerun finalF7 CPUtest. RequiredUIflow correction verification. Featureclippy once stable. Need dynamic tests editor SaveAs/import/export/cliptrigger/audio and V8 metrics plus required landing checks; do not claim complete/mainavailable. Update docs/beads exactverifiedscope, reviewdiff and commitexactpaths. Untracked handoff staysoutofgit. No optional broad GPU explorations/sweeps; namedobservations only.
