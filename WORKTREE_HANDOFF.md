BUG-qh04: isolated FLOWER probe inconclusive
Current branch codex/rt-stage-diagnostics tip261cfb487 is LOCAL ONLY and UNLANDED. Raw branch push blocked by Codex guard (app delivery requires land_branch.py); do not bypass. Source experiment: warmup_probe test behindgpu-proofs, MANIFOLD_REPRO_PROJECT external fixture, targetlayer53b8a585. MANIFOLD_REPRO_SERIAL_AS=1 drains GPU preparation and ASbuild/refit separately; compiled only in gpu-proofs. Both one-shot runs passed: baseline24RTdispatches10.60s, ordered29dispatches10.74s,0faults,Quiescent,2orderedfences. Hence no reproduced failure and no timing-cause conclusion. Focused compile+clippy passed. Full GPU/landing gate NOT rerun because this remains an unfinished diagnostic experiment. No more reruns without new evidence/scope.
Fixture /private/tmp/manifold-rt-diagnostic/Corrosion - FLOWER only.manifold (parentgroup+failinglayer firstclip; settings/presets retained). Logs flower-baseline.log and flower-ordered.log adjacent. Original unchanged. Next useful direction: restore preceding warmup/load context in automated harness; no user manual rerun needed.
Existing release target/release/manifold is earlier60bf9869d diagnostic build, already landed green; original user diagnostic crash session1788757657784-44352. Root remains open in BUG-qh04; see bead for precise evidence. User critically low usage, concise bounded work.

Latest: ae7ac2e1b adds MANIFOLD_SKIP_WARMUP=1 (release-compatible). Skips run_warmup after quality setup, clears progress, logs warning. First-use loading remains. Local only/offmain. App clippy passed;348 app tests passed3ignored. Release binary rebuilt with this commit successfully. Next user test command uses MANIFOLD_SKIP_WARMUP=1 MANIFOLD_GPU_DIAGNOSTICS=1 RUST_LOG=info RUSTC_WRAPPER= cargo run --release -p manifold-app --manifest-path absolute slot-3/Cargo.toml. No agent project test.

Latest user outcome: NO CRASH with warmup disabled in release (single user-reported run; logs not independently checked). Recorded in open BUG-qh04. Prioritize warmup workload/timing and resource lifecycle; not proof of overload or final fix. Preserve bypass as temporary workaround.
