Transform input note (unused by the command — scripts/rt_region_probe.py reads the two
PNG captures from $WORKFLOW_RUN_DIR directly). This step rules the bleed and control
legs of RAYTRACING_DESIGN.md section 11.4 MB-B's gate and emits the pin threshold for
MB-C.
