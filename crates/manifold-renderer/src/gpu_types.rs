//! GPU resource handle types — legacy file.
//!
//! All GPU types are now provided by `manifold_gpu`. This module is kept
//! as a re-export shim for any remaining internal references.
//!
//! Content-thread code should use `manifold_gpu::GpuTexture` directly.
