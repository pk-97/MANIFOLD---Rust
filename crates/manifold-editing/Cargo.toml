[package]
name = "manifold-editing"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
manifold-core = { path = "../manifold-core" }

[dev-dependencies]
manifold-io = { path = "../manifold-io" }
inventory = "0.3"
serde_json = { workspace = true }

[lints]
workspace = true
