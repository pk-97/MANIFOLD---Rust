[workspace]
resolver = "2"
members = [
    "manifold-analyzer-dsp",
    "manifold-analyzer-gui",
    "manifold-analyzer-plugin",
    "manifold-analyzer-cli",
    "xtask",
]

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
